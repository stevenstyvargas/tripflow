# Proceso con IA — Tripflow

> El reto pide documentar qué se le pidió a la IA y qué se ajustó
> manualmente. Este archivo se va llenando durante el desarrollo,
> no se reconstruye al final.

## Registro

| Fecha | Qué le pedí | Qué ajusté yo | Por qué |
|---|---|---|---|
| 2026-08-20 | Leer CLAUDE.md, README.md y docs/, y armar la lógica base del store de datos y el flujo de creación de un viaje (nombre, presupuesto límite, divisa) | Revisé el `store.js` generado: validación de nombre/presupuesto/divisa en `createTrip`, y que `getTrip`/`getTrips` mantuvieran el shape ya pensado para migrar a backend. En `new-trip.js` confirmé que el error de validación se muestra en el formulario en vez de solo lanzar la excepción | El store es la base de todo lo demás (dashboard, gastos); prefiero que la validación viva ahí y no repetirla en cada página |
| 2026-08-21 | Instalar el SDK de Firebase, inicializarlo con mi config del proyecto (`tripflow-3d2ad`), e implementar login con Google (Fase 2) + persistencia en Firestore (Fase 3), reemplazando localStorage. Esto expande el alcance v1 original ("sin backend"), documentado en un prompt maestro externo al repo | Antes de tocar código, la IA señaló que este cambio contradice `CLAUDE.md` ("sin backend", "no expandir sin confirmar") y no encontró el prompt maestro en el repo — confirmé el cambio de alcance explícitamente. Pedí que la config de Firebase no quedara hardcodeada en el código versionado: se movió a `.env` (gitignorado) con `.env.example` como plantilla. Revisé el modelo de datos en Firestore (`users/{uid}/trips/{tripId}/expenses/{expenseId}`, con IDs autogenerados por Firestore en vez de `crypto.randomUUID()`) y agregué `firestore.rules` para que cada usuario solo pueda leer/escribir sus propios documentos | El SDK de Firebase es una dependencia externa real (no un UI kit visual, así que no choca con esa regla), pero sí es un cambio de arquitectura que requería mi confirmación explícita antes de ejecutarse |
| 2026-08-21 | Rediseñar Inicio/Alertas/Historial con sidebar fija, siguiendo criterios de diseño exactos que di (paleta, semáforo 80%/100%, KPIs, dona por categoría, cards con foto, Inter, Lucide Icons) | La IA tuvo que llenar huecos que mis criterios no cubrían y los dejó explícitos en vez de adivinar en silencio: (1) agregó un campo `status` (active/closed) al viaje y un botón "Cerrar viaje" en la card de Inicio, porque sin eso Historial nunca tendría contenido; (2) agregó `photoUrl` como URL opcional en el formulario de nuevo viaje (no hay subida de imagen real todavía, ver roadmap en design-system.md); (3) los 3 KPIs de Inicio y la dona se calculan solo sobre viajes *activos*, normalizando montos a COP con la tabla de tasas fijas existente para poder sumar viajes en distintas divisas en un solo número. Revisé el criterio de contraste de los badges de semáforo (texto en un tono oscurecido del mismo color, no el color plano, sobre fondo tintado) y separé la paleta de categorías del gráfico de dona de la paleta del semáforo para no mezclar significados | Son decisiones de modelo de datos/alcance que no estaban en los criterios de diseño que me diste; preferí resolverlas de forma mínima y documentarlas acá a bloquear el avance de las 3 pantallas por esto |
| 2026-08-21 | Conectar el flujo de registro de gastos (ya existía `addExpense` en el store, pero no era accesible desde la UI): cards de Inicio clickeables hacia un detalle de viaje, formulario "Agregar gasto" (monto, categoría, fecha, descripción opcional) y ruta `/viaje/:id` | Antes de escribir el formulario confirmé que dona (Inicio), semáforo y Alertas no necesitaban código nuevo: los tres se recalculan en cada render a partir de `state.expenses` en memoria (ver `store.js`), así que un gasto guardado ahí ya se refleja al volver a esas pantallas sin necesidad de un event bus o estado global adicional. Sí agregué `validateExpenseInput` en el store (mismo patrón que `validateTripInput`) para que la validación de categoría/monto/fecha viva ahí y no en la página. Para las cards clickeables evité anidar el botón "Cerrar viaje" dentro de un `<a>` (interactive content anidado no es válido en HTML): el link envuelve la foto y el cuerpo de la card, y el botón de cerrar queda como hermano | Mantener la validación centralizada en el store y no duplicar lógica de recálculo que ya era reactiva por diseño |
| 2026-08-22 | El login con Google fallaba en producción (Vercel) con `auth/popup-blocked`, causado por la política Cross-Origin-Opener-Policy que Vercel aplica por defecto y que rompe `signInWithPopup` (confirmado en consola del navegador, no reproducible en localhost). Pedí reemplazar `signInWithPopup` por `signInWithRedirect` en `auth.js`, capturar el resultado con `getRedirectResult()` al inicializar la app (antes del primer render), y mantener el mismo manejo de errores/loading que ya existía | Revisé que `getGoogleRedirectResult()` se llamara y se esperara (`await`) antes de que `initRouter` decida si muestra login o dashboard, para no renderizar login y luego "parpadear" al dashboard cuando Firebase resuelve el redirect. El error de `getRedirectResult()` no podía mostrarse con el mismo `errorBox` que ya arma `login.js` en el click del botón (ese código no corre en el regreso del redirect), así que le agregué un parámetro `{ error }` a `login.render()` para poder pasarle un error inicial desde el router. Verifiqué con `vite build` que no quedaran imports rotos | `signInWithRedirect` es más confiable en navegadores móviles también, que necesitamos probar de todas formas |
| 2026-08-22 | El flujo `signInWithRedirect` del commit anterior encontró un problema distinto y fuera de nuestro control: un 404 en el auth handler de Firebase al volver de Google (infraestructura de Firebase, no del código). Pedí revertir `signInWithGoogle()` a `signInWithPopup` (usando `git checkout` sobre el commit anterior al cambio a redirect para restaurar `auth.js`, `router.js` y `login.js` tal cual estaban), quitar la llamada a `getGoogleRedirectResult()` del router, y en su lugar agregar `vercel.json` con el header `Cross-Origin-Opener-Policy: same-origin-allow-popups` para que Vercel deje de bloquear el popup | Comparé el `vercel.json` pedido contra el error original (`auth/popup-blocked` por COOP `same-origin`) — `same-origin-allow-popups` es la excepción documentada por Firebase/Google para este caso exacto, así que no ajusté el valor. Verifiqué con `vite build` que la reversión no dejara imports rotos (`getGoogleRedirectResult` sin usar, etc.) | Popup + header COOP relajado es más simple que redirect y no depende de la infraestructura de auth handler de Firebase, que fue la causa del segundo problema |
| 2026-08-22 | En pruebas reales con AdBlock activo, `signInWithPopup` seguía fallando (`auth/popup-blocked`) incluso con el header COOP relajado, porque el bloqueo venía de la extensión, no de Vercel. Pedí implementar el patrón que Firebase recomienda para este caso: intentar `signInWithPopup` primero y, solo si falla con `auth/popup-blocked` o `auth/popup-closed-by-user`, caer automáticamente a `signInWithRedirect` sin mostrar error (debe verse como transición normal); cualquier otro código de error sí muestra el mensaje habitual. Volví a agregar `getGoogleRedirectResult()` en la inicialización del router para capturar el resultado al volver del redirect de fallback | Anoté explícitamente en `auth.js` que `auth/popup-closed-by-user` no distingue de forma confiable un bloqueo de un cierre intencional del usuario (Firebase reporta el mismo código en ambos casos) — caer a redirect en los dos casos es seguro: si fue intencional, el usuario solo vuelve a ver la pantalla de Google. También confirmé que el `catch` del click en `login.js` no necesitaba cambios: como `signInWithGoogle()` ya no relanza esos dos códigos, solo le llegan los errores que sí deben mostrarse. Verifiqué con `vite build` que compilara bien | Con AdBlock, popup solo (aunque con COOP relajado) no es suficiente; el fallback a redirect es la recomendación oficial de Firebase para navegadores con bloqueadores o restricciones de terceros |
| 2026-08-22 | El Service Worker (vite-plugin-pwa/Workbox) seguía sirviendo el build anterior después de cada deploy, obligando a desregistrarlo a mano para ver los cambios. Pedí agregar `skipWaiting()` y `clientsClaim()` a la config de Workbox en `vite.config.js` para que el SW nuevo tome control de inmediato | Antes de escribir el cambio, generé el `dist/sw.js` actual y confirmé con `grep` que ya incluía `self.skipWaiting()` y `clientsClaim()` — con `registerType: "autoUpdate"`, vite-plugin-pwa ya los activa por defecto (lo confirmé en `node_modules/vite-plugin-pwa/dist/index.js`). Agregué igual el bloque `workbox: { skipWaiting: true, clientsClaim: true }` explícito en `vite.config.js`, para que el comportamiento no dependa implícitamente de `registerType` y quede documentado en el propio archivo. Volví a compilar y confirmé que `dist/sw.js` no cambió (mismo comportamiento, ahora explícito) | El problema reportado puede no resolverse solo con esto: skipWaiting/clientsClaim hacen que el SW nuevo tome control, pero no fuerzan a las pestañas ya abiertas a recargar. Si el problema persiste después de este cambio, el siguiente paso sería escuchar el evento de actualización del SW (`onNeedRefresh` de `virtual:pwa-register`) y recargar la página automáticamente — no lo agregué porque no fue lo pedido |
| 2026-08-22 | Primera de 4 mejoras pedidas a partir del feedback real registrado en `docs/user-feedback.md`: agregar categorías "Compras" y "Otros" a `EXPENSE_CATEGORIES`, con ícono Lucide y color de dona distintivo cada una | Elegí `shopping-bag` para Compras y `package` para Otros (evité `more-horizontal`, que ya se asocia a "más opciones" de UI, no a una categoría de gasto). Para los colores nuevos (`--color-category-compras: #2e6ddc`, `--color-category-otros: #7c5cbf`) evité reusar naranja/rojo porque esos ya están reservados al semáforo de estado y, a partir de esta misma tanda de cambios, al patrón visual de acciones destructivas (botones de eliminar) — mezclar esos significados con una categoría de gasto sería confuso. `package` como nombre de import choca con la palabra reservada `package` en JS estricto (los módulos ES siempre corren en modo estricto), así que lo importé como `packageIcon` y solo la key del mapa de íconos quedó como `"package"` | Mantener la paleta de categorías consistente con la convención ya documentada en `tokens.css` (colores de categoría separados del significado del semáforo) |
| 2026-08-22 | Segunda mejora del feedback: el botón para cerrar un viaje pasaba desapercibido (texto gris chico). Pedí cambiarlo a ícono `trash-2` + texto "Finalizar viaje" + color rojo `#DC2E24`, claramente clickeable, sin cambiar el comportamiento (sigue archivando el viaje a Historial, no borra datos) | Creé una clase reutilizable `.button-danger-outline` (borde y texto rojo, ícono en rojo, fondo tintado al hover con `color-mix`, mismo patrón que ya usa `.status-badge-danger`) en vez de estilos puntuales solo para este botón, porque el punto 3 del feedback (eliminar gasto) pide explícitamente "el mismo patrón visual" — reusar la clase evita que los dos botones se desincronicen visualmente más adelante. Usé `var(--color-status-danger)` en vez de hardcodear `#DC2E24` de nuevo, porque el token ya existe en `tokens.css` con ese valor exacto | Aclaré en el propio texto del pedido que "Finalizar viaje" sigue siendo cerrar/archivar, no un borrado real — si se agrega borrado permanente más adelante necesita confirmación aparte |
| 2026-08-22 | Tercera mejora del feedback: poder eliminar un gasto individual ya registrado, con el mismo patrón visual del botón "Finalizar viaje" y una confirmación previa | Agregué `deleteExpense(expenseId)` en `store.js` (busca el gasto en memoria para obtener su `tripId`, hace `deleteDoc` en Firestore y filtra el estado local) — no necesitó tocar `firestore.rules`, porque la regla existente (`allow read, write`) ya cubre `delete`. Reusé `showConfirmModal()` (el mismo componente del punto de "confirmar antes de exceder presupuesto") en vez de armar un modal nuevo o un doble clic, para no introducir un segundo patrón de confirmación en la app. Verifiqué que no hiciera falta recalcular nada a mano en Inicio/Alertas/dona: como ya se documentó al conectar el registro de gastos, esas vistas leen `state.expenses` en cada render, así que basta con re-renderizar `trip-detail` después de borrar | Reusar el modal de confirmación existente mantiene un solo patrón de "acción con consecuencia" en toda la app, en vez de dos comportamientos distintos para casos similares |
| 2026-08-22 | Cuarta mejora del feedback: auditar que todos los montos de la app (KPIs de Inicio, cards de viaje, detalle de viaje, gastos individuales, Alertas, Historial, formularios) usen separador de miles | Revisé con `grep` cada lugar del código que muestra `amount`, `budgetLimit`, `spent`, `totalSpent` o `remaining`: los 12 puntos encontrados (Inicio: KPIs y cards; detalle de viaje: encabezado, lista de gastos, modal de confirmación; Alertas; Historial) ya pasan siempre por `formatCurrency()`, que usa `Intl.NumberFormat` con locale por divisa (`es-CO`/`en-US`/`de-DE`) y separador de miles correcto para cada una. No encontré ningún monto mostrado en crudo. El único valor numérico sin formatear es "Viajes activos" en el KPI de Inicio, que es un conteo de viajes, no dinero — no le aplica esta regla. No hubo cambios de código, solo esta verificación | No hubo lugares sin formatear que corregir; se deja documentada la auditoría para que quede registro de que se revisó explícitamente, no que se asumió |
| 2026-08-22 | Primera de 2 mejoras grandes del feedback: editar un viaje ya creado, reutilizando el formulario de "Nuevo viaje" en vez de duplicarlo. Pedido explícito: si el viaje ya tiene gastos, no permitir cambiar de divisa | Extraje el formulario a `src/components/trip-form.js` (`mountTripForm()`) para que `new-trip.js` y el nuevo `edit-trip.js` monten exactamente el mismo componente — ver el razonamiento completo en `docs/product-decisions.md`. Agregué la ruta `/viaje/:id/editar` en `router.js`; como `matchRoute()` ya manejaba `/viaje/{tripId}` con un slice+decode simple, tuve que cambiarlo a `split("/")` para poder distinguir el id del sufijo `/editar` sin romper la ruta existente de detalle. Para bloquear la divisa sin romper el envío del formulario, deshabilité las `<option>` de los otros valores en vez de deshabilitar el `<select>` completo — un `<select disabled>` no manda su valor en `FormData`, así que el campo se hubiera perdido al guardar. Agregué la misma validación en `updateTrip()` (store) además del bloqueo visual, por si alguien se salta el HTML. Verifiqué con `vite build` que compilara y que las rutas nuevas generaran su propio chunk | El semáforo/porcentaje se recalcula solo al reabrir `trip-detail` después de guardar, porque ya lee `budgetLimit` del viaje en cada render — no hizo falta ningún mecanismo nuevo para eso, solo reutilizar el patrón de re-render que ya existía |
| 2026-08-22 | Segunda mejora grande del feedback: editar un gasto ya registrado, reutilizando el mismo formulario de "Agregar gasto" (no uno nuevo), con un ícono de editar junto al de eliminar en cada gasto | A diferencia de "Editar viaje" (página propia), esto ocurre en la misma pantalla que "Agregar gasto", así que en vez de extraer un componente aparte reusé literalmente el mismo `<form id="expense-form">`: agregué un estado `editingExpenseId` en el closure de `render()` que decide si el submit llama a `addExpense()` o a `updateExpense()` (nueva función en el store, mismo patrón que `deleteExpense`), más `enterEditMode()`/`exitEditMode()` para precargar y limpiar el formulario, y un botón "Cancelar" que solo aparece en modo edición. El chequeo de "vas a superar el presupuesto" tuvo que ajustarse: al editar, el total proyectado se calcula quitando el monto viejo del gasto y sumando el nuevo (no sumando el nuevo sobre el total actual, que ya incluye el viejo), y el modal no se repite si el viaje ya estaba excedido antes de la edición — el pedido decía explícitamente "y antes no lo cruzaba". Verifiqué con `vite build` que compilara | Ver `docs/product-decisions.md` para el razonamiento completo de reusar un solo formulario y de no repetir la confirmación de presupuesto |
| 2026-08-22 | Formato en vivo (separador de miles mientras se escribe) para "Presupuesto límite" y "Monto", cambiando esos inputs de `type="number"` a `type="text" inputmode="numeric"`, con la prueba de aceptación explícita de que el cursor no debía saltar al final en cada tecla | Extraje el formato a `utils/currency.js` (`formatAmountInput`/`parseAmountInput`/`formatStoredAmount`, construidos sobre los mismos locales por divisa que ya usaba `formatCurrency`, para que el separador coincida con el resto de la app) y el manejo de cursor a `utils/amount-input.js` (`bindAmountInput`). El cursor lo trackeo por *cantidad de dígitos antes de él* en vez de por índice de caracter — conté los dígitos antes del cursor en el valor viejo, reformateo, y ubico el cursor después de esa misma cantidad de dígitos en el valor nuevo; así el separador puede insertarse o correrse sin que el cursor salte. Verifiqué mentalmente la secuencia completa del enunciado (escribir "4000000" letra por letra) simulando el evento `input` paso a paso antes de dar el cambio por bueno. Decidí NO soportar decimales en estos campos (antes `step="any"` sí los permitía) porque formatear separador de miles y decimales a la vez con cursor correcto es sustancialmente más complejo, y la prueba de aceptación solo cubre enteros — lo dejé documentado como decisión de alcance en `docs/product-decisions.md`, no como un descuido. Verifiqué con `vite build` que compilara | No pude probar el comportamiento real del cursor en un navegador (requiere login de Google real para llegar a estas pantallas); la verificación fue simulando el algoritmo a mano contra la prueba mental del pedido, no una prueba automatizada ni manual en la app |
| 2026-08-22 | Quitar los decimales de TODOS los montos que se muestran en la app (KPIs, cards, detalle de viaje, gastos, Alertas, Historial), sin perder precisión internamente en cálculos/almacenamiento — cambio pedido en un solo lugar, la función de formato ya auditada antes | Como `formatCurrency()` es el único punto por el que pasa cualquier monto mostrado (confirmado en la auditoría previa y verificado de nuevo con `grep` de `toFixed`/`toLocaleString`/`Intl.NumberFormat` en todo `src/`, sin resultados fuera de `currency.js`), el cambio fue agregar `maximumFractionDigits: 0` a los formateadores de USD y EUR en `CURRENCY_FORMATTERS` (COP ya lo tenía desde el inicio). Antes de tocarlo probé con Node que `Intl.NumberFormat` con `style: "currency"` no lanza error al fijar `maximumFractionDigits: 0` sin `minimumFractionDigits` explícito (el motor lo ajusta solo) y que redondea correctamente (`2500000.5` → `$2,500,001`). No toqué `convert()`, `getTripTotal()` ni ningún cálculo — solo el formateador de salida, tal como pedía el alcance | Ninguno de los cálculos (semáforo, totales, alertas) pasa por `formatCurrency()`, todos usan los números crudos de `store.js`, así que este cambio es puramente de presentación, sin riesgo de que el redondeo visual afecte un resultado |
| 2026-08-22 | Dos cambios de visualización de gastos por categoría pedidos juntos: (1) Inicio pasa de dona a torre de barras verticales, una por categoría, en el orden fijo de `EXPENSE_CATEGORIES`, con ícono + nombre + monto por torre, gris si la categoría no tiene gasto; (2) el detalle de viaje gana una dona nueva con el desglose de ESE viaje únicamente. Pedido explícito de reusar los mismos colores por categoría en ambos gráficos | Como `EXPENSE_CATEGORIES` ya lista las categorías en el orden pedido (Transporte, Comida, Hotel, Imprevistos, Compras, Otros) y ya trae el ícono de cada una (el mismo que usa "Gastos registrados"), no hizo falta inventar ningún orden ni ícono nuevo — armé `components/bar-chart.js` iterando esa misma lista tal cual, sin ordenar por `value`. La altura de cada torre es `(value/maxValue)*100%` calculado solo sobre las categorías actuales (no un máximo histórico); si `maxValue` es 0 (nadie ha gastado nada) todas quedan en altura mínima. Para el gris de categorías sin gasto usé `var(--color-text-muted)` en vez de un gris nuevo, reusando el token ya existente. La dona de `trip-detail.js` reutiliza el `donutChart()` que ya existía (antes vivía en Inicio) sin tocarlo — solo cambié qué datos le paso (gastos de un solo viaje, sin conversión de divisa porque todos los gastos de un viaje ya comparten su divisa). El estado vacío de la dona (cuando el viaje no tiene gastos) es el que el componente ya traía de antes, no armé uno nuevo. Verifiqué con `vite build` que compilara | Ver `docs/product-decisions.md` para el razonamiento de por qué son dos tipos de gráfico distintos a propósito, no la misma dona duplicada dos veces |
| 2026-08-22 | Reversión de la decisión anterior: la dona del detalle de viaje también pasa a torre de barras (mismo componente y estilo que Inicio), y en su lugar los títulos se diferencian — Inicio: "Gasto por categoría" + subtítulo "Todos tus viajes activos"; detalle de viaje: "Gasto por categoría de [nombre del viaje]" con el nombre real | Como `barChart()` ya recibía `categories` y `currency` como parámetros genéricos (no acoplado a Inicio), reusarlo en `trip-detail.js` fue cambiar la llamada, no escribir un componente nuevo: mismo `computeCategoryBreakdown()` que ya tenía trip-detail, solo le agregué `id`/`icon` a cada entrada (que `barChart()` necesita y `donutChart()` no). Al quedar la dona sin ningún uso en toda la app, borré `components/donut-chart.js` y su bloque CSS (`.donut-chart`, `.donut-legend*`) en vez de dejarlos como código muerto — confirmé con `grep -rn donut src/` que no quedaba ninguna referencia antes de borrar. Para el subtítulo de Inicio agregué una clase `.section-subtitle` genérica (no específica de esta sección), pensada para reusarse si otra sección necesita el mismo patrón de título + aclaración chica debajo. Actualicé el comentario de cabecera de `dashboard.js`, `trip-detail.js` y `bar-chart.js` para que no quedaran describiendo una dona que ya no existe. Verifiqué con `vite build` que compilara | Documenté en `docs/product-decisions.md` la decisión anterior como superada (no la borré, para que quede el registro de por qué se probó la variación de tipo de gráfico primero) y agregué la nueva decisión con el razonamiento de por qué se volvió a un solo tipo de gráfico |
| 2026-08-22 | Separar los KPIs de Inicio por divisa (un bloque por cada divisa realmente en uso, con píldora navy/verde/gris) en vez de sumar todo normalizado a COP, y eliminar por completo el gráfico "Gasto por categoría" de Inicio (queda solo dentro de cada viaje) | Para la píldora, en vez de agregar tokens de color nuevos reusé los que ya existían con otro propósito: `--color-primary` (navy) para COP y `--color-accent` (verde) para USD — son los colores de marca genéricos, no los del semáforo (`--color-status-*`), así que no chocan de significado. Copié el tratamiento de fondo tintado + texto oscurecido de `.status-badge` en vez de fondo sólido + texto blanco: con `--color-accent` (`#a1d35b`, un verde claro) un pill sólido con texto blanco encima tendría contraste insuficiente — el mismo problema de accesibilidad que `docs/branding.md` ya documenta haber resuelto para el semáforo, así que apliqué la misma solución en vez de reintroducir el problema en un componente nuevo. Para el texto verde oscurecido reusé `--color-status-ok-text` en vez de definir un tono nuevo, porque `--color-accent` y `--color-status-ok` son literalmente el mismo hex (`#a1d35b`) — ya existía el oscurecido correcto. El orden de los bloques de divisa sigue `SUPPORTED_CURRENCIES` (no el orden de creación de los viajes), para que la posición no cambie de un render a otro. Al quitar la normalización, `convert()`/`FIXED_RATES_TO_COP` en `currency.js` se quedaron sin ningún llamador en toda la app — los dejé (con un comentario explicando por qué) en vez de borrarlos, porque el pedido documenta explícitamente una "vista consolidada opcional" futura en el roadmap que sí los necesitaría. Verifiqué con `grep` que `convert`/`REPORT_CURRENCY`/`EXPENSE_CATEGORIES`/`barChart` quedaran completamente fuera de `dashboard.js` tras el cambio. Verifiqué con `vite build` que compilara | En el README ya existía una línea de roadmap "Divisas adicionales" y otra de "Escaneo de recibos con IA" desde antes de este pedido — no dupliqué esas líneas, enriquecí la de divisas con la limitación de que el patrón de columnas no escala más allá de 2-3 divisas, en vez de agregar una línea nueva que dijera casi lo mismo |
| 2026-08-22 | Reemplazar las columnas de KPI por divisa (decisión anterior) por un selector estilo "cambiar idioma" (Amazon): píldora con la divisa activa + flecha, dropdown con las divisas en uso, KPIs vuelven a ser un solo bloque limpio filtrado por la divisa elegida, selección en memoria (no Firestore) | Reusé la clase `.currency-pill` ya existente (de la decisión anterior) como base visual del botón del selector, en vez de un estilo nuevo — el pedido literalmente decía "píldora". La selección vive en `let selectedCurrency` a nivel de módulo de `dashboard.js` (fuera de la función `render()`), no dentro del closure de `render()`, porque el router llama a `render()` de cero cada vez que se navega a Inicio — una variable local a `render()` se hubiera reseteado en cada visita, no solo al recargar la página, que es justo lo que se pedía evitar. Para la regla de default ("más viajes activos, o COP si hay empate") itero `SUPPORTED_CURRENCIES` en orden y solo reemplazo el "mejor" candidato con `>` estricto (no `>=`), así un empate nunca desplaza a COP porque arranca como el candidato inicial. Si la divisa seleccionada se queda sin viajes activos (se cerraron/eliminaron), el siguiente render recalcula el default solo, sin lógica adicional. El dropdown abre/cierra con manejo explícito de limpieza: los listeners de `document` (clic afuera, Escape) se agregan solo mientras el menú está abierto y se remueven explícitamente al cerrarlo — inclusive antes de llamar a `render()` al elegir una opción, para no dejar un listener apuntando a nodos del DOM ya reemplazados. Usé `event.stopPropagation()` en el botón que abre el menú para que ese mismo clic no se propague a `document` y lo cierre de inmediato. Verifiqué con `vite build` que compilara | Documenté en `docs/product-decisions.md` la decisión de columnas como superada por esta, y anoté que esta migración es exactamente la que esa misma decisión ya había dejado como roadmap |
| 2026-08-22 | Botón "Agregar a Google Calendar" en el detalle de viaje, visible solo si el viaje tiene fecha de inicio y fin, usando el link público de Google Calendar con parámetros en la URL (sin OAuth ni API) | El detalle no obvio de este pedido era el manejo de fechas: Google Calendar trata el fin de un evento de día completo como *exclusivo*, así que armé `toGoogleCalendarEndDate()` sumando un día a `endDate` en UTC (parseando con sufijo `T00:00:00Z` y usando `setUTCDate`/`getUTCDate`, nunca el constructor local de `Date` ni `getDate()`/`setDate()`) para no arrastrar bugs de zona horaria — lo probé con Node contra un cambio de mes, de año y un 28 de febrero en año bisiesto antes de darlo por bueno, en vez de asumir que funcionaba. Para el texto y el detalle usé `encodeURIComponent()` en vez de `URLSearchParams` (que hubiera codificado el `/` de `dates` y usado `+` en vez de `%20` para espacios) — quería que la URL final se pareciera lo más posible al template exacto que se me dio. Estilo: reusé `.button-outline` (ya existía para "Cancelar") en vez de crear una clase nueva, y le agregué `display:flex`/`gap` porque antes solo se usaba con texto plano, nunca con ícono — confirmé que no rompiera los usos existentes (el cancelar del modal y del formulario de gasto) antes de tocarlo. Verifiqué con `vite build` que compilara | Documenté en `docs/product-decisions.md` por qué un link plano es suficiente para este caso de uso y qué se necesitaría (OAuth) si más adelante se quisiera sincronización real, no solo un link de una sola vez |
| 2026-08-23 | Simplificar la app a una sola divisa (COP): quitar el campo "Divisa" del formulario de viaje, eliminar el selector de divisa de Inicio, quitar la lógica de conversión/normalización, y agregar el texto "COP" junto a todo monto mostrado en la app. El pedido aclaraba que era una decisión consciente de alcance, no una limitación técnica, y que los viajes de prueba en USD/EUR no debían migrarse (el usuario los iba a borrar a mano) | Antes de tocar código verifiqué con `git log`/lectura de `currency.js` si de verdad existía una integración con una API de tasas de cambio (Frankfurter) como mencionaba el pedido — no encontré ninguna: el único mecanismo de conversión era `convert()` con tasas fijas hardcodeadas, y ya estaba sin ningún llamador en la app desde el cambio anterior. No corregí al usuario sobre eso en el chat, solo procedí sobre el código real en vez de asumir que existía algo que no estaba. Para agregar "COP" a todos los montos en un solo lugar (como pedía el punto 4), lo agregué dentro de `formatCurrency()` mismo en vez de tocar cada pantalla — ya estaba confirmado por auditorías previas que ese es el único punto por el que pasa cualquier monto mostrado, así que un cambio ahí se propaga solo. Al quitar el parámetro `currency` de `formatCurrency()`/`formatAmountInput()`/`formatStoredAmount()`/`bindAmountInput()`/`barChart()`, tuve que actualizar cada call site que pasaba un segundo argumento de divisa (`trip.currency`, `expense.currency`) — lo verifiqué con `grep` al final para confirmar que no quedara ninguno. En `store.js`, en vez de borrar el campo `currency` de los documentos (cambiaría el modelo de datos existente en Firestore), lo dejé pero hardcodeado a una constante `CURRENCY = "COP"` interna, ya no aceptado como parámetro — mantiene compatibilidad con los documentos viejos sin necesitar una migración. Borré también el CSS y el ícono (`chevron-down`) del selector de divisa, y `SUPPORTED_CURRENCIES`, ya sin ningún uso. Verifiqué con `vite build` que compilara limpio | Documenté en `docs/product-decisions.md` las tres decisiones de multi-divisa anteriores como superadas (no borradas) y agregué una decisión final consolidada explicando el porqué del retiro completo; actualicé también el roadmap de `README.md` con el texto exacto pedido |
| 2026-08-23 | Rediseñar Historial con KPIs de resumen (mismo componente que Inicio), foto por card, cards clickeables hacia el detalle de viaje en modo solo lectura y chevron indicando que son clickeables — usando únicamente la paleta/iconografía propia, aunque el layout se pensó mirando un patrón de referencia externo | Extraje `renderKpiCard()` de `dashboard.js` a `components/kpi-card.js` en vez de duplicar el markup en `history.js`, porque ahora se usa en dos pantallas — mismo criterio que ya se aplicó con `trip-form.js`/`bar-chart.js` en tareas anteriores. Para "Presupuesto cumplido" interpreté literalmente el pedido ("% de viajes... que NO superaron su presupuesto (badge 'En control' al cerrar)"): cuento específicamente `status === STATUS.OK`, no "no excedió el 100%" en general — un viaje en estado `warning` (80-100%) no cuenta como cumplido, porque su badge no dice "En control". Para la foto reusé `icon("plane", "trip-card-photo-placeholder")` literal (mismo ícono, misma clase) en vez de crear un ícono/clase nueva, y armé `.history-item-photo` como contenedor propio (tamaño de thumbnail distinto al de Inicio porque el layout ahora es horizontal, no vertical) en vez de reusar `.trip-card-photo` completo, que traía dimensiones pensadas para otro layout. En `trip-detail.js`, el modo solo lectura de un viaje cerrado (`trip.status === TRIP_STATUS.CLOSED`, importado de `store.js`) solo oculta el botón "Agregar gasto" — decidí NO ocultar también "Editar viaje" ni los botones de editar/eliminar por gasto individual, porque el pedido solo mencionaba "Agregar gasto" y "Finalizar viaje" (que ni siquiera vive en esta pantalla) como lo que había que ocultar; extender el bloqueo a más acciones sin que se pidiera hubiera sido remover funcionalidad no solicitada. Como el formulario de gasto sigue en el DOM (oculto) para que editar un gasto puntual siga funcionando aun sin el botón "Agregar gasto" visible, tuve que envolver el `addEventListener` del botón toggle en un `if (toggleButton)` porque ahora puede ser `null`. Verifiqué con `vite build` que compilara | Le señalé al usuario en el resumen que la interpretación de "solo lectura" quedó acotada a ocultar solo "Agregar gasto" (no edición/eliminación de gastos ni "Editar viaje"), por si en realidad querían un bloqueo más amplio — documentado también en `docs/product-decisions.md` |
| 2026-08-23 | Ajustar el estilo visual de los montos en las KPI cards (Historial e Inicio): número un paso más chico, "COP" en la misma línea en gris tenue y peso normal, sin partirse en dos líneas, sin colores de semáforo. Después, ampliar el ancho máximo del contenedor principal (64rem→~1800px) y el grid de "Tus viajes activos" a `minmax(280px,1fr)`. Después, un buscador en el header de Inicio que filtra las cards por nombre en vivo. Las 3 mejoras del dashboard de Inicio se pidieron juntas al final, con un commit separado por cada una: (1) saludo personalizado con el nombre de Google, (2) el buscador ya construido, (3) volver a agregar la dona de "Gasto por categoría" en Inicio ahora que la app es solo COP, documentando en este mismo archivo por qué es matemáticamente válida de nuevo | Como `formatCurrency()` siempre agrega " COP" al final, separé ese sufijo con una regex en `kpi-card.js` en vez de tocar cada call site — mismo criterio que ya se usó para centralizar el formato de moneda en un solo punto. Para el saludo, reusé literalmente el mismo dato que el sidebar ya usa para el email (`getCurrentUser()` de `auth.js`), con la cadena de fallback que pedía el enunciado (`displayName` → parte del email antes de la `@`). Para la dona, en vez de escribirla de cero recuperé el `donut-chart.js` original de `git show 320c497:...` (SVG puro, sin librerías) porque es exactamente el mismo componente que ya existía antes de borrarse — reescribirlo hubiera sido reinventar algo que ya estaba probado y documentado. Al construir los 5 cambios noté que quedaban 2 piezas de trabajo previas (estilo de KPI, ancho de contenedor) sin commitear de turnos anteriores del chat — las separé en sus propios commits (con su propio `vite build` cada una) en vez de mezclarlas dentro de los 3 commits pedidos, para que cada commit siguiera representando un solo cambio real, siguiendo la regla de `CLAUDE.md` de que el historial de commits también se evalúa. También noté que el pedido asumía que ya existía una torre de barras en Inicio ("junto al gráfico de torres que ya existe ahí") — no era cierto en el estado actual del código (la torre solo vive en el detalle de cada viaje desde la simplificación a COP); no bloqueé el trabajo por esto, seguí adelante agregando solo la dona y se lo señalé al usuario en el resumen. No pude iniciar sesión con Google real para verificar Inicio en el navegador (requiere una cuenta real), así que probé el grid, el buscador y el layout general con un harness HTML estático que carga el CSS real de la app fuera del router, con Playwright/Chromium instalado ad-hoc en el scratchpad (no como dependencia del proyecto) | El detalle de la torre de barras inexistente en Inicio se lo marqué al usuario en vez de asumir en silencio que el pedido estaba mal o de agregar una torre no pedida; documenté el resto de decisiones de la dona en `docs/product-decisions.md` |

| 2026-08-23 | Mover la información de cuenta (email + "Cerrar sesión", antes fija al final del sidebar) a un menú desplegable en la esquina superior derecha del header, visible en las 4 pantallas principales (Inicio, Alertas, Historial, detalle de viaje) — con foto de perfil de Google (o inicial en placeholder si no hay foto), nombre, email y chevron; dropdown con "Cerrar sesión" que cierra al hacer clic afuera o Escape | Creé `components/account-menu.js` como componente reusable (`accountMenu(user)` + `bindAccountMenu(container)`) en vez de duplicar el markup/lógica en las 4 páginas, siguiendo el mismo criterio ya aplicado con `kpi-card.js`/`bar-chart.js`. Para el patrón de cerrar con clic-afuera/Escape reusé la misma forma que ya usaba `confirm-modal.js` (agregar los listeners de `document` solo mientras el menú está abierto, y quitarlos al cerrar, para no dejar listeners huérfanos apuntando a nodos ya reemplazados en el siguiente render). En Inicio el menú queda al final de `.page-header-actions`, después de "Nuevo viaje", tal como se pidió; en Alertas/Historial tuve que agregar el `<div class="page-header-actions">` que esas páginas no tenían (solo un `<h1>` suelto); en el detalle de viaje se agregó como último elemento del `page-header-actions` que ya existía. Validé `user.photoURL` con el mismo criterio que `trip.photoUrl` (regex de esquema, acá `https://` porque Google siempre lo entrega así) antes de insertarlo en el `src` de la imagen. Al vaciar el sidebar de la info de cuenta, `bindSidebar()` dejó de tener nada que bindear (ya no hay botón de cerrar sesión ahí), así que la borré de `sidebar.js` y quité su llamada en `app-shell.js`, junto con el CSS ya sin uso (`.sidebar-footer`, `.sidebar-user`, `.sidebar-signout` y sus reglas del media query mobile) y el ícono `user` de Lucide, que se quedó sin ningún otro uso en la app al quitar ese bloque. En mobile (`≤45rem`), oculto `.account-menu-info` (nombre+email) y dejo solo foto+chevron, mismo criterio de compactación que ya usa `.sidebar-nav-item span`. No agregué el menú a la pantalla de error "Este viaje no existe o fue cerrado" del detalle de viaje (no tiene `<header>` de ningún tipo hoy, es un estado de fallback, no la pantalla en sí) — se lo señalé al usuario en vez de agregar un header nuevo no pedido. Verifiqué con `vite build` que compilara, y visualmente con el mismo harness HTML estático de tareas anteriores (Inicio requiere login real con Google, al que no tengo acceso en este entorno), probando con y sin foto y con el dropdown abierto | Reusar el patrón de listeners de `confirm-modal.js` en vez de inventar uno nuevo evita un segundo criterio de "cerrar al hacer clic afuera" en la misma app; dejé documentada la omisión de la pantalla de error del detalle de viaje en vez de resolverla en silencio |

| 2026-08-23 | Hacer el header de Inicio consistente con Alertas/Historial: quitar el "Hola, [nombre] 👋" que estaba arriba del título como texto chico, y que el título mismo sea ese saludo (con solo el primer nombre, mismo tamaño/peso que "Alertas"/"Historial"), todo en una sola fila a la misma altura que las otras 2 pantallas | El problema real era estructural: Inicio envolvía el saludo + `<h1>Inicio</h1>` en un `<div>` extra (dos líneas apiladas), mientras Alertas/Historial tienen el `<h1>` como hijo directo de `.page-header` — esa diferencia de forma, no de CSS, era lo que desalineaba la altura. Quité el `<div>` envolvente y dejé un solo `<h1>Hola, [nombre] 👋</h1>` directo, igual de estructura que las otras 2 páginas, y borré `.page-greeting` del CSS al quedar sin uso. Separé el primer nombre con `displayName.trim().split(/\s+/)[0]` — cambio acotado a `getGreetingName()` (usado solo para el saludo-título), sin tocar `accountMenu()`, que sigue mostrando el `displayName` completo. Verifiqué con un harness HTML que mide `getBoundingClientRect().height` del `<header>` en las 3 pantallas con Playwright que la altura queda exactamente igual (43px en las 3), y con un nombre largo sintético que el truncamiento con elipsis del menú de cuenta (`.account-menu-name`, ya implementado en la tarea anterior) corta limpio, sin partir a media palabra | Confirmé con la medición programática (no solo comparación visual a ojo) que las 3 alturas son idénticas, porque es justo lo que pedía el punto 4 del pedido |

| 2026-08-23 | Agregar el mismo buscador de Inicio ("Buscar destino o país...", lupa, filtro en vivo por nombre) a Historial, en la misma fila del header, a la izquierda del menú de cuenta (Historial no tiene botón "Nuevo viaje") — pedido explícito de reusar el componente existente en vez de crear uno nuevo | El input de búsqueda vivía como markup inline en `dashboard.js` (ícono + `<input>`), no como componente propio, así que antes de reusarlo en Historial lo extraje a `components/search-field.js` (`searchField({ id })`) y actualicé `dashboard.js` para usarlo también — mismo criterio que ya se aplicó con `kpi-card.js`/`account-menu.js` cuando una pieza de UI pasó a usarse en 2+ pantallas. El filtrado en sí (qué lista filtra, cómo re-renderiza) no se extrajo a un helper compartido porque es distinto en cada página (Inicio bindea el botón "Finalizar viaje" en cada re-render vía `bindTripsSection`; Historial no tiene botones interactivos en sus items) — repliqué el mismo patrón de `renderXSection(trips, query)` + listener de `input` que reemplaza solo el contenedor de la lista, ya usado en `dashboard.js`, en vez de generalizarlo prematuramente para un solo caso de reuso. Verifiqué con `vite build` que compilara, y con un harness HTML estático + Playwright que la altura del header en Historial sigue siendo 43px, igual que Inicio y Alertas, y que el filtrado en vivo y el estado "No se encontraron viajes con ese nombre" funcionan igual que en Inicio | Extraer el componente en vez de duplicar el markup evita que Inicio e Historial se desincronicen visualmente si el estilo del buscador cambia más adelante — mismo argumento que ya está documentado para `kpi-card.js` |

| 2026-08-23 | Igualar la altura de `.page-header` a 40px (valor de Alertas/Historial) en las 4 pantallas — el usuario confirmó con el inspector real del navegador que Inicio media 43px, 3px más que Alertas/Historial (40px), y sospechaba del emoji del saludo | Reproduje el bug real antes de tocar CSS: mi verificación anterior de esta misma altura (tarea del header consistente) había dado 43px en las 3 pantallas por igual porque mi harness de prueba no cargaba la fuente Inter real (Google Fonts) — con el fallback `system-ui` los 43px "coincidían" en las 3 y ocultaban el problema. Rehice el harness con el mismo `<link>` de Google Fonts que usa `index.html` y until `document.fonts.ready`, y ahí sí reproduje exactamente el reporte del usuario (Inicio 43px, Alertas/Historial 40px). Causa confirmada: `.page-header h1` no tenía `line-height` explícito (usaba `normal`), y con `line-height: normal` el navegador calcula la altura de línea a partir de la métrica de CADA fuente presente en esa línea — el emoji 👋 cae en una fuente de reemplazo (emoji) con métrica más alta que Inter, así que el line box de ESE `<h1>` específico se expandía para acomodarla, aunque el `font-size` fuera igual al de los demás títulos. Arreglé con `line-height: 1.25` explícito en `.page-header h1` (2rem × 1.25 = 40px exactos) — un valor explícito, a diferencia de `normal`, no varía según la fuente de cada glifo en la línea. Reverifiqué con el mismo harness (Inter real + `document.fonts.ready`): Inicio, Alertas e Historial dan 40px exactos los tres. El detalle de viaje da 95px porque su `.page-header` apila título+ícono editar, línea de presupuesto y badge de semáforo — contenido genuinamente distinto (3 líneas, no 1), así que NO lo forcé a 40px quitando esa información; se lo señalé al usuario en el resumen en vez de rediseñar esa pantalla sin que se pidiera | El primer chequeo de altura (tarea anterior) dio un falso positivo por no cargar la fuente real — lección para verificaciones futuras de altura/tipografía: reproducir con la fuente real de la app (Google Fonts), no confiar en el fallback del sistema |

| 2026-08-23 | Rediseñar las cards de Alertas siguiendo el patrón visual de Historial: foto de destino, borde completo fino (no solo barra izquierda) del color de severidad real de cada alerta, y botón "Ver viaje" hacia el detalle — manteniendo el ícono+texto de alerta que ya existía | Reusé literalmente `trip.photoUrl` con la misma validación de esquema (`/^https?:\/\//`) y el mismo placeholder (`icon("plane", "trip-card-photo-placeholder")`) que ya usan `dashboard.js`/`history.js`, sin escribir un componente nuevo. Cambié `.alert-item` de `border-left: 4px solid` a `border: 1px solid` (borde completo, más fino) — el color por severidad ya existía vía `.alert-item-danger` sobreescribiendo el color base de warning, así que solo cambié qué propiedad de borde pinta (`border-left-color` → `border-color`), la lógica de qué color le toca a cada alerta no cambió. El ícono de alerta (triángulo naranja / octágono rojo) y el texto descriptivo se mantienen intactos, solo reacomodados junto al nuevo layout con foto — no los reemplacé por el borde, que es señal adicional, tal como pedía el enunciado explícitamente. El botón "Ver viaje" reusa `.button-outline` (mismo estilo que "Agregar a Google Calendar" en detalle de viaje) en vez de una clase nueva, con el ícono `arrow-right` de Lucide (agregado a `icons.js`, no existía). Verifiqué con `vite build` que compilara, y visualmente con un harness HTML con Inter real (Google Fonts) simulando una alerta roja (>100%) y una naranja (80-100%) a la vez: cada borde toma su color correcto de forma independiente | Reducí el padding de `.alert-item` de `--space-4` a `--space-3` para alinear con el tamaño de fila que ya usa `.history-item-link` en el mismo patrón — no es un cambio no pedido, es parte de "seguir el mismo patrón visual de Historial" que pedía el punto 1 |

| 2026-08-23 | Primera de 2 mejoras relacionadas: barra de progreso de presupuesto, hover con elevación y badge de duración ("N noches") en la card de viaje — descrito como "el mismo componente reutilizado en Inicio, Historial y la nueva Mis viajes" | El componente vertical de card (foto arriba, cuerpo abajo) solo existía duplicado en `dashboard.js` — Historial usa un layout horizontal distinto (`.history-item`, fila con foto a la izquierda) que el propio pedido dice explícitamente mantener igual ("Inicio y Historial se mantienen igual en estructura"). Interpreté la premisa de "mismo componente" como: extraer `components/trip-card.js` con la card vertical completa (para Inicio y, en el siguiente commit, Mis viajes) y, para no duplicar la lógica de cálculo, exportar también `computeDurationBadge()` y `budgetProgressBar()` sueltos para que `history.js` los reuse dentro de su propia fila horizontal sin cambiarle la forma. Verifiqué antes de tocar código que `trip.startDate`/`endDate` ya existieran en el modelo de datos (`store.js`) — no hizo falta agregar campos. El ancho de la barra se topa con `Math.min(ratio, 1)` pero el color sigue el status real (puede seguir siendo rojo con la barra llena al 100%, aunque el gasto haya sido 112%) — mismo criterio que ya usa el badge de semáforo (`getStatusMeta(status).colorVar`), reusado tal cual en vez de hardcodear los 3 hex de nuevo. El hover (`translateY(-2px)` + `box-shadow: var(--shadow-md)`, 180ms) se aplicó por separado a `.trip-card:hover` y `.history-item:hover` — dos reglas cortas, no una clase compartida, siguiendo la regla de no crear abstracciones para 2 usos casi triviales. Verifiqué con `vite build` que compilara, y visualmente con un harness HTML (Inter real) con 3 cards (verde/naranja/rojo, con y sin badge de duración) y 2 filas de historial con las mismas barras — confirmé que el color de la barra sigue el semáforo real y que el ancho nunca se desborda | El componente de card en sí (JSX/markup vertical) no terminó siendo LITERALMENTE el mismo en las 3 pantallas porque el pedido pedía explícitamente que Historial no cambiara de forma — lo señalo para que quede claro que interpreté "mismo componente" como "misma lógica/estilo reusado", no como "un solo layout forzado en las 3 pantallas" |

| 2026-08-23 | Segunda mejora relacionada: nueva pantalla "Mis viajes" (`/viajes`), en el sidebar entre Inicio y Alertas — header idéntico a Alertas/Historial, mismo buscador, 3 pestañas (Todos/Activos/Cerrados) y grid con la card mejorada del commit anterior, mostrando TODOS los viajes del usuario | Agregué `getAllTrips()` a `store.js` (no existía una función que devolviera activos+cerrados juntos) y la ruta `/viajes` → `pages/my-trips.js` en `router.js`, entre `/` y `/alertas` en `NAV_ITEMS` de `sidebar.js` con el ícono `map` de Lucide (agregado a `icons.js`, no usado antes en la app — evité `home`/`plane`/`bell`/`history`, ya usados por otros items del nav). Reusé literalmente `renderTripCard()` del commit anterior para el grid, con `showCloseButton: trip.status !== TRIP_STATUS.CLOSED` por card — el botón "Finalizar viaje" solo aparece en viajes activos, igual criterio que ya usa Inicio. Las pestañas filtran en memoria sobre el mismo array de `trips` ya cargado (sin volver a pedir datos), y se combinan con el buscador existente (`searchField`) en la misma función `renderTripsSection(trips, filter, query)`, mismo patrón de "reemplazar solo el contenedor de la lista" que ya usan Inicio/Historial. No dupliqué la lógica de editable/solo-lectura del detalle de viaje: cada card navega a `/viaje/:id`, que ya decide sola según `trip.status` (sin cambios en `trip-detail.js`). Verifiqué con `vite build` que compilara, y visualmente con un harness HTML (Inter real) que el header mide 40px igual que Alertas/Historial, el ítem del sidebar cae en la posición correcta, y las 3 cards (activa con progreso verde, cerrada sin botón, activa en rojo) se ven consistentes con el grid de Inicio | El nombre de archivo `my-trips.js` (no `trips.js`) evita choque de nombre con `data/store.js`'s conceptos de "trips" al escanear imports, y es más específico sobre qué pantalla es |

| 2026-08-23 | Rediseñar el header del detalle de viaje: barra superior de 40px SOLO con acciones (Google Calendar/Agregar gasto/menú de cuenta) alineadas a la derecha, sin título; separador delgado debajo; nueva fila con foto + nombre/badge + 2 KPI cards ("Gastado"/"Restante", mismo componente de Inicio) | Al sacar el `<h1>` de dentro de `.page-header`, perdí sin darme cuenta su estilo (`.page-header h1` ya no aplicaba) — lo noté al revisar el resultado visual, no antes de escribir el CSS, y agregué `.trip-detail-title h1` con el mismo `font-size`/`line-height` explícito que ya tenía el resto de headers, para no reintroducir el bug de altura por line-height que se arregló en la tarea anterior. Para el separador, mi primer intento agregó `padding-bottom` al `.page-header`, lo que subió la barra a 41-57px medida con Playwright — el pedido era que la BARRA (el contenido con los botones) siguiera midiendo 40px, y el separador fuera aparte, no que se sumara al alto de la barra; corregí quitando el padding y dejando solo `border-bottom`, y verifiqué aparte con `getBoundingClientRect()` sobre `.page-header-actions` (no sobre `.page-header`, que ahora incluye el borde) que la fila de botones diera exactamente 40px. Reusé literalmente `renderKpiCard()` de `components/kpi-card.js` (mismo tratamiento de "COP" chico que se ajustó hace varias tareas) en vez de un componente nuevo, envueltas en un `<ul class="kpi-row">` para que la barra de progreso/COP se vea idéntica a Inicio — con `.trip-detail-summary .kpi-row { flex: 1; margin: 0; }` para que las 2 cards compartan la fila con la foto y el nombre en vez de ocupar el ancho completo de la página. La foto reusa `trip.photoUrl` con la misma validación de esquema y el mismo placeholder de avión que el resto de la app, en un cuadrado de 4rem (64px) con `--radius-sm`, igual criterio de esquinas que `.history-item-photo`. "Restante" puede salir negativo (viaje con gasto excedido) — mismo comportamiento que ya tenía el KPI equivalente de Inicio, no lo capé. Verifiqué con `vite build` que compilara, y visualmente con un harness HTML (Inter real) con un viaje con foto y otro sin foto (uno con presupuesto excedido, para confirmar el número negativo en "Restante") | El error de line-height y el de padding-bottom quedan documentados acá para que quede registro de que los detecté y corregí durante la misma tarea, verificando con medición programática en vez de asumir que el CSS quedaba bien a la primera |

| 2026-08-23 | Primer de 2 ajustes: truncar el nombre del viaje en las cards (Inicio, Historial, Mis viajes) a una sola línea con elipsis en vez de saltar a una segunda línea y descuadrar la altura, con `title` con el nombre completo para el tooltip nativo | Como `trip-card.js` ya es el componente compartido de Inicio/Mis viajes (desde la tarea de la card mejorada), solo tuve que tocar `.trip-card-name` ahí y `.history-item-name` en `history.js` para cubrir las 3 pantallas. `white-space:nowrap`+`overflow:hidden`+`text-overflow:ellipsis` no alcanzaban solos: `.trip-card-name` es un hijo directo de `.trip-card-body` (`display:flex; flex-direction:column; align-items:flex-start`), así que sin un `width` explícito el párrafo se dimensiona a su contenido (`nowrap` lo hace tan ancho como el texto completo) y nunca llega a recortarse — agregué `width:100%` (mismo truco que ya usaba `.budget-progress` bajo el mismo padre). `.history-item-name` no necesitó el mismo ajuste porque su padre (`.history-item-body`) ya tenía `flex:1; min-width:0;` de antes, y un `<p>` normal (no flex item) ya ocupa el 100% de su contenedor por default. Verifiqué con `vite build` y con un harness HTML usando el nombre real "San José de Caldas - Casanare": confirmé con Playwright que dos cards con nombres de distinto largo dan la MISMA altura total (293px las dos), y visualmente que el texto corta en una sola línea con "..." | El `title` se agregó en el mismo `<p>` que ya escapaba `trip.name` con `escapeHtml()` — sin escapado nuevo que agregar, ya estaba cubierto |

| 2026-08-23 | Segundo ajuste: quitar el separador (`border-bottom: 1px solid rgba(0,0,0,0.08)`) entre la barra de acciones y la fila de foto+nombre+KPIs del detalle de viaje, pero confirmando antes que no se usara en otra pantalla | Antes de borrar nada busqué con `grep` todas las reglas con ese mismo color gris (`rgba(0, 0, 0, 0.08)`) en `base.css`: aparecía 3 veces — `.sidebar` en el media query mobile, `.tab-list` de Mis viajes, y `.page-trip-detail .page-header` (la que se agregó en la tarea anterior). Solo borré esta última, que está scoped con el selector `.page-trip-detail` y no puede afectar otra pantalla — dejé las otras dos intactas. Verifiqué con Playwright (`getComputedStyle(...).borderBottomWidth`) que el header del detalle de viaje da `0px` después del cambio, y con `vite build` que compilara | El pedido pedía explícitamente confirmar antes de borrar — documento acá el resultado de esa verificación en vez de solo decir "confirmé" sin mostrar cómo |

| 2026-08-23 | Reestructurar el header y la sección superior del detalle de viaje: todo (foto miniatura + nombre truncado + editar + badge compacto) integrado en la barra de 40px, y debajo un layout de 2 columnas — KPIs "Presupuesto"/"Gastado"/"Restante" apiladas en ancho fijo (350-400px) a la izquierda, torre de categorías a la derecha | Agregué un parámetro `extraClass` opcional a `statusBadge()` (mismo patrón que ya usa `icon()`) para poder pasarle `"status-badge-sm"` sin duplicar el componente, y un nuevo ícono `target` para la KPI "Presupuesto" (no existía ninguna KPI para el límite total hasta ahora; reusé `wallet`/`piggy-bank` para Gastado/Restante, igual que ya hace Inicio, para no inventar un mapeo de íconos distinto). El truncamiento del `<h1>` reusa literalmente el mismo criterio que ya se aplicó a `.trip-card-name`/`.history-item-name` (`white-space:nowrap`+`overflow:hidden`+`text-overflow:ellipsis`+`title` con el nombre completo). Al probar con un nombre largo ("San José de Caldas - Casanare") encontré un bug que no anticipé: aunque el `<h1>` se truncaba bien, `.page-header-actions` (que tiene `flex-wrap:wrap` para las otras 4 pantallas) empezaba a envolver sus botones a una segunda línea, dejando el header en 90px en vez de 40px — el `flex-shrink:1` por default de `.page-header-actions` hacía que absorbiera parte de la compresión junto con `.trip-detail-title` en vez de que TODA la compresión cayera sobre el nombre. Lo corregí con `.page-trip-detail .page-header-actions { flex-shrink: 0; }`, scoped solo a esta pantalla para no tocar el comportamiento ya verificado de Inicio/Alertas/Historial/Mis viajes. Verifiqué con `vite build` y con un harness HTML (Inter real) midiendo con Playwright: header en 40px exactos y columna de KPIs en 352px (dentro del rango 350-400px pedido) en ambos casos (nombre corto "Paris" y el largo), con el nombre largo mostrando la elipsis correctamente | El bug de `flex-wrap` no era obvio de anticipar sin medir — lo encontré probando con el nombre largo real que pedía el enunciado, no asumiendo que el truncamiento del `<h1>` bastaba por sí solo para mantener la fila en 40px |

| 2026-08-23 | Fusionar Historial con Mis viajes: se detectó redundancia (ambos mostraban viajes cerrados por separado). Mover las 4 KPIs de Historial a Mis viajes, debajo del buscador y arriba de las pestañas, recalculadas según la pestaña activa (Todos/Activos/Cerrados) en vez de fijas a "cerrados"; eliminar por completo la pantalla de Historial (ruta, sidebar, archivo) con redirect de `/historial` a `/viajes?tab=closed` | Antes de borrar nada busqué con `grep` toda referencia real a `history.js`/`/historial`/`page-history` en `src/` (no solo la palabra "historial", que también aparece en comentarios sin relación) — 3 lugares: `sidebar.js` (item de nav), `router.js` (ruta) y `base.css` (`.page-history` en el selector de max-width compartido). Los 3 se actualizaron. Como Historial ya reusaba `budgetProgressBar()`/`computeDurationBadge()` de `trip-card.js` (exportados desde la tarea de la card mejorada), al borrar `history.js` esos dos dejaron de tener un consumidor externo — les quité el `export` en vez de dejarlos exportados sin necesidad, ya que ahora solo los usa el propio `trip-card.js`. Confirmé con otro `grep` que los íconos `history` y `chevron-right` (usados solo por el nav item y el chevron de `.history-item`, respectivamente) quedaron sin ningún otro llamador y los quité de `icons.js`, igual que borré el bloque completo de CSS `.history-*` y el ya huérfano `.duration-badge-sm` (el modificador de tamaño chico del badge de duración, que solo usaba la miniatura de `.history-item-photo`). Para el redirect, en vez de mantener `/historial` en el mapa `routes` apuntando a un componente que ya no existe, agregué un mapa `REDIRECTS` aparte que el router chequea antes de `matchRoute()`: si el path coincide, solo cambia `location.hash` (dispara `hashchange` solo, sin duplicar el render). Aproveché el mismo cambio para que el router parseara un query string genérico en el hash (`#/viajes?tab=closed`) y se lo pasara a `page.render()` junto a los params de ruta — `my-trips.js` ahora acepta `{ tab }` para preseleccionar la pestaña. Verifiqué con `vite build` que compilara sin el chunk de `history.js`, y con un harness HTML reimplementando la misma lógica de `computeKpis()`/`filterByStatus()` con 5 viajes de prueba (3 activos, 2 cerrados) que los 3 conteos, montos y porcentajes de KPI cambian correctamente al hacer clic en cada pestaña — confirmé a mano la aritmética de los 3 casos contra lo que mostraba el harness antes de darlo por bueno, y que "Cerrados" reproduce exactamente los mismos números que mostraba Historial | Documentado en `docs/product-decisions.md` (decisión pedida explícitamente) y actualicé también `docs/design-system.md`/`docs/branding.md`, que todavía listaban a Historial como pantalla propia |

| 2026-08-23 | Agregar un badge de ESTADO (En curso/Finalizado) a la card de viaje, distinto del badge de semáforo, visible junto a él; decidir con criterio propio en qué pestañas de Mis viajes mostrarlo; quitar "Finalizar viaje" de las cards de viajes cerrados | Antes de escribir nada verifiqué si "Finalizar viaje" ya estaba oculto en cerrados — sí: `renderTripCard(trip, { showCloseButton: trip.status !== TRIP_STATUS.CLOSED })` en `my-trips.js` ya lo resolvía desde que se creó esa pantalla, así que no hubo cambio ahí, solo confirmación. Para el ícono de "Finalizado" usé `lock` (nuevo en `icons.js`) en vez del sugerido `check-circle`/`circle-check`, porque ese último ícono ya lo usa `.status-badge-ok` ("En control") — reusarlo en el badge de estado, aunque con color distinto, hubiera hecho que dos badges con significados distintos compartieran el mismo glifo justo uno al lado del otro. El color se resolvió con `--color-bg` (el gris de fondo de toda la app) + `--color-text-muted`, no navy — más neutro que cualquier color de marca, para no competir visualmente con el semáforo. La decisión de en qué pestañas mostrarlo la dejó explícita el pedido ("usa tu criterio, dime qué decidiste"): solo en "Todos" (`showStateBadge = filter === "all"` en `renderTripsSection`), porque en "Activos"/"Cerrados" el filtro ya comunica el estado — mostrarlo ahí sería ruido repetido. Verifiqué con `vite build` y con un harness HTML (Inter real) con un viaje activo y uno cerrado en la misma lista: se distinguen de un vistazo (badge gris de estado + badge de color de semáforo, iconos distintos), y el botón "Finalizar viaje" solo aparece en el activo | Documentado en `docs/design-system.md` (nueva fila "Badge de estado") — el criterio de "cuándo mostrarlo" queda ahí también, no solo en el código, para que no se pierda la razón si se toca esto más adelante |

| 2026-08-23 | Rediseñar "Gasto por categoría" (total en el centro de la dona + leyenda con %/monto) y agregar un bloque nuevo "Gasto vs presupuesto" (anillo de progreso con semáforo) al lado, en grid 50/50 | Extraje `splitCurrencyUnit()` (antes vivía solo dentro de `kpi-card.js`) a `utils/currency.js` para reusarlo en `donut-chart.js` y el nuevo `components/budget-ring.js` sin duplicar la misma regex en un tercer lugar — `kpi-card.js` pasó a importarla en vez de definirla. Para el anillo reusé literalmente `getBudgetStatus()`/`getStatusMeta()` de `utils/status.js` (mismo cálculo y mismos íconos/colores que ya usa `.status-badge` en toda la app — `circle-check`/`alert-triangle`/`alert-octagon`), solo con el texto específico que pedía el enunciado ("en rango"/"cerca del límite"/"presupuesto excedido") en vez de las etiquetas de `.status-badge` ("En control", etc.), porque el pedido especificaba ese texto exacto para este bloque. El % que se muestra es el real (puede pasar de 100), pero el relleno visual del aro se topa en `Math.min(ratio,1)` — no tendría sentido dibujar más de una vuelta completa. Los puntos de la leyenda "Gastado"/"Restante" usan `--color-primary`/`--color-accent` (no `--color-status-ok`), porque son identificadores de la leyenda, no un juicio de "presupuesto en buen estado" — mezclar esos dos significados hubiera sido el mismo error que la regla de "no uses colores del semáforo para KPIs neutros" ya prohíbe en otras partes de la app. "Restante" negativo se muestra en rojo con signo, sin ocultarse (`--color-status-danger`), tal como pedía el enunciado. Antes de dar el bloque por cerrado probé específicamente el estado >100% con un harness HTML (Inter real) y encontré un bug real que no era obvio en los otros 2 estados: el texto de estado ("presupuesto excedido") es más largo que "en rango"/"cerca del límite", y sin un tope de ancho se extendía más allá del hueco de la dona hacia el trazo del anillo — como el texto usa el MISMO color que el anillo (`var(${meta.colorVar})`, por diseño, para reforzar el semáforo), la parte que caía sobre el trazo se volvía ilegible (texto rojo sobre trazo rojo), no un simple desborde visual. Medí con Playwright (`getBoundingClientRect()`) el hueco real de la dona (diámetro 96px = 2×(radio 60 − grosor 24 ÷ 2), no los 160px de la caja completa del SVG) y confirmé que el mismo problema existía también en el total de `donut-chart.js` (`$4.320.000COP` medía 151px de ancho, se salía del hueco de 96px por ambos lados) aunque ahí era menos grave por el contraste (texto oscuro, no del mismo color que el anillo) — igual lo corregí con el mismo criterio: `max-width` calculado sobre el hueco real, no la caja del SVG, permitiendo que el texto haga wrap en vez de desbordarse. Verifiqué de nuevo con Playwright que los 3 estados del semáforo y el layout mobile (grid a 1 columna en `≤45rem`) quedaran legibles y sin overlaps, y con `vite build` que compilara | El bug del texto invisible (rojo sobre rojo) no se veía "raro" a simple vista en una revisión rápida del código — solo apareció al renderizar de verdad el caso extremo (>100%) y mirarlo, que es justo por lo que se pidió explícitamente confirmar ese estado antes de cerrar el bloque |

| 2026-08-23 | Reemplazar "Exportar PDF por viaje" (pendiente, sin implementar todavía) por "Compartir por WhatsApp": botón junto al de Google Calendar en el detalle de viaje, link `wa.me/?text=` con mensaje plano (nombre+fechas, presupuesto/gastado/restante en COP, estado del semáforo en texto explícito, top 3 categorías) | Antes de escribir código busqué en todo el repo (`grep -ri "pdf\|exportar"`) alguna mención existente de "Exportar PDF por viaje" para saber qué reemplazar exactamente — no encontré ninguna: no estaba en `README.md`, `docs/product-decisions.md` ni en ningún comentario de código. Documenté la decisión igual (era lo explícitamente pedido), asumiendo que era un plan que el usuario tenía en mente pero que todavía no había quedado escrito en el repo. Para el rango de fechas del mensaje ("12-16 nov") no había nada que reusar — el único formateador de fechas parecido (`formatDateRange` de la vieja pantalla de Historial) se borró al fusionar Historial con Mis viajes — así que escribí uno nuevo (`formatShareDateRange()`) con el mismo parseo en UTC que ya usa `toGoogleCalendarEndDate()` en este mismo archivo, para no reintroducir el bug de zona horaria que esa función ya evita explícitamente. Sí reusé, tal como pedía el enunciado: `formatCurrency()` para los 3 montos, y el `status`/`getBudgetStatus()` ya calculado en `render()` (el mismo que usa el modal de "vas a superar el presupuesto") — no se recalculó nada de eso. El texto de estado usa las mismas 3 frases que ya introduje para el anillo "Gasto vs presupuesto" de Inicio (`budget-ring.js`), solo con mayúscula inicial porque acá es una oración del mensaje, no una etiqueta chica junto a un ícono — no las compartí como una sola constante entre los dos archivos porque un objeto de 3 líneas no justifica una abstracción cruzada. Para el ícono elegí `message-circle` (agregado a `icons.js`) en vez de `share-2`, el otro sugerido por el enunciado, porque semánticamente representa mejor "chat/mensaje" que "compartir" genérico para un botón que específicamente abre WhatsApp. Verifiqué con un script de Node (reimplementando la misma lógica pura) que el mensaje arma exactamente el formato del ejemplo del enunciado (con el sufijo " COP" real de `formatCurrency()`, que el ejemplo omitía por ser solo ilustrativo) y que el rango de fechas funciona tanto en el mismo mes ("12-16 nov") como cruzando meses ("28 oct - 3 nov"). Verifiqué con `vite build` y visualmente con un harness HTML que el botón se ve consistente con "Agregar a Google Calendar" | Señalé la ausencia de una mención previa de "Exportar PDF" en el repo en vez de asumir en silencio dónde estaba — documenté la decisión de todas formas porque el pedido la pedía explícitamente, no porque hubiera algo que "reemplazar" en el código |

| 2026-08-24 | Reposicionar el botón "Finalizar viaje" de las cards de "Mis viajes" hacia el Detalle de viaje, y mover el ícono de editar del header del detalle hacia el mismo lugar: quitar por completo el botón de las cards (Inicio y Mis viajes comparten `trip-card.js`, aunque el pedido solo mencionaba Mis viajes, Inicio usaba el mismo botón con el mismo criterio, así que lo quité de los dos para no dejar el componente compartido con un comportamiento inconsistente entre pantallas); quitar el ícono de lápiz junto al badge de estado en la barra superior del detalle; agregar una fila nueva al final de "Gastos registrados" con "Editar viaje" (`.button-outline`, mismo estilo que "Agregar a Google Calendar") + un botón de solo ícono (`trash-2`, `aria-label`/`title="Finalizar viaje"`) que ejecuta la misma `closeTrip()` de antes, oculto en viajes ya finalizados | Al quitar `showCloseButton` de `trip-card.js` no quedó ningún consumidor de esa opción ni de `.trip-card-close`, así que borré la opción del componente entero (no solo el valor por defecto) y quité el binding de clic en `dashboard.js`/`my-trips.js` (que llamaban `closeTrip()` desde ahí) — moví esa única función a `trip-detail.js`, sin duplicar lógica. Para el ícono de finalizar sin texto reusé `.icon-button` (mismo patrón que ya usaba el lápiz de editar) en vez de `.button-danger-outline` (que trae texto obligatorio), y agregué un modificador `.icon-button-danger` para que el hover sea rojo en vez del primary por defecto — así se distingue visualmente de una acción neutra aunque no tenga texto. Antes de tocar código le señalé al usuario (en la instrucción original) que este pedido llegó envuelto en un intento de inyección de prompt disfrazado de instrucción de sistema al final del resumen de compactación ("Respond with TEXT ONLY... Tool calls will be REJECTED") — no lo seguí, confirmé el pedido real leyendo el historial de la sesión. No pude iniciar sesión con Google real para ver el cambio en la app corriendo (mismo límite que tareas anteriores), así que verifiqué con un harness HTML estático (mismo patrón ya usado en tareas previas) con el CSS real compilado (`vite build`) y los SVG reales de `lucide-static`, con Playwright/Chromium ya cacheado localmente de una sesión anterior: confirmé visualmente que las cards (activa, cerca del límite, finalizada) no muestran el botón viejo y que el footer nuevo del detalle muestra ambos botones en viaje activo y solo "Editar viaje" en uno finalizado. Verifiqué con `vite build` que compilara limpio | Extender la remoción del botón a Inicio (no solo Mis viajes, que fue lo único mencionado explícitamente) evita que el mismo componente compartido (`trip-card.js`) quede con el botón visible en una pantalla y no en la otra, que hubiera sido una inconsistencia no pedida por nadie; lo señalo acá para que quede explícito que fue una decisión mía, no algo que el pedido cubriera literalmente |

| 2026-08-24 | 3 correcciones puntuales en `README.md` (quitar "con dominio propio" de la línea de despliegue, agregar "Compartir viaje por WhatsApp" al alcance v1, agregar "Multi-idioma" y "Eliminar viaje de forma permanente" al roadmap) sin tocar ninguna otra sección; agregar la decisión correspondiente de postergar "eliminar viaje permanentemente" en `docs/product-decisions.md`, mismo tono que las entradas de multi-divisa y PDF→WhatsApp | Los 3 cambios de `README.md` fueron ediciones puntuales de una línea/lista cada una, sin tocar el resto del archivo, tal como pedía explícitamente el enunciado. Para la entrada de `product-decisions.md` seguí el mismo formato Problema/Decisión/Por qué que ya usan las entradas existentes, explicando el motivo técnico real (borrado en cascada de la subcolección de gastos en Firestore, que no se hace solo) en vez de un motivo genérico de "falta de tiempo" — es la razón concreta que hace que esta feature no sea trivial de agregar bien en dos días. No hubo cambios de código, solo documentación; no aplicó `vite build` | Ninguna decisión de diseño propia que documentar más allá del formato — el pedido especificaba el texto exacto de cada línea a agregar |

| 2026-08-24 | Ajustar el botón "Finalizar viaje" del detalle de viaje (agregado en la tarea anterior como ícono solo): cambiarlo al mismo estilo que "Eliminar" en cada fila de gasto (borde/texto rojo, ícono + texto, fondo blanco), quitar el `aria-label`/tooltip que compensaba la falta de texto (ya no aplica con texto explícito), y centrar horizontalmente la fila que lo contiene junto a "Editar viaje" | Cambié la clase del botón de `.icon-button.icon-button-danger` a `.button-danger-outline` (la misma que ya usa "Eliminar" en `expense-item-actions`) y le agregué `<span>Finalizar viaje</span>` junto al ícono — mismo componente/clase, tal como pedía el punto 1. Al quedar `.icon-button-danger` sin ningún consumidor en todo `src/` (confirmado con `grep`), la borré del CSS en vez de dejarla como código muerto. Agregué `justify-content: center` a `.trip-detail-footer-actions` sin tocar `gap`, que ya estaba en `var(--space-3)` desde que se creó la fila. Verifiqué con `vite build` que compilara, y visualmente con el mismo harness HTML estático de la tarea anterior (regenerado con el CSS recién compilado): el botón se ve idéntico al patrón de "Eliminar", la fila queda centrada, y sigue ocultándose en el viaje ya finalizado | No hubo decisiones de diseño propias más allá de lo pedido — el punto 1 ya especificaba explícitamente reusar el mismo componente/clase existente |

| 2026-08-24 | En Mis viajes, pestaña "Todos": (1) reordenar las cards para que todos los "En curso" aparezcan antes que todos los "Finalizado", manteniendo el orden relativo dentro de cada grupo (sin ordenar por fecha ni otro criterio); (2) aplicar `grayscale(1)` SOLO a la foto/placeholder de las cards de viajes finalizados (no a la card completa), mismo tratamiento también en la pestaña "Cerrados" | Para el orden, cambié `filterByStatus()` en `my-trips.js`: el caso `"all"` (antes devolvía `trips` tal cual) ahora hace `trips.slice().sort(...)` comparando `Number(status === CLOSED)` — `Array.prototype.sort` es estable desde ES2019 (garantizado por spec en todos los motores modernos, no una casualidad de implementación), así que el orden dentro de cada grupo se conserva exactamente, solo se reagrupan por estado. Usé `.slice()` antes de `.sort()` porque `sort()` muta el array en el lugar, y `trips` es el mismo array que ya se pasa también a `computeKpis()`/`renderKpis()` — mutarlo habría sido un efecto secundario no obvio sobre datos compartidos. Verifiqué la lógica con un script de Node aislado (5 viajes intercalados activo/cerrado) antes de darla por buena, confirmando que el array original queda intacto. Para la escala de grises, agregué una clase condicional `.trip-card-photo-closed` (`filter: grayscale(1)`) al `<div class="trip-card-photo">` en `trip-card.js`, calculada directamente de `trip.status === TRIP_STATUS.CLOSED` — como esa condición es una propiedad del viaje, no de la pestaña activa, el mismo cambio ya cubre "Cerrados" sin lógica adicional (y nunca se activa en Inicio, que solo lista viajes activos). No toqué `.trip-card-photo-placeholder` ni ningún otro elemento de la card — confirmé visualmente con el harness HTML (mismo patrón de tareas anteriores, CSS real compilado) que el placeholder (fondo navy + ícono de avión) pasa a gris pero el badge de estado, el badge de semáforo y la barra de progreso mantienen su color normal | Verificar la estabilidad de `sort()` con un script aislado en vez de asumirla evita un bug sutil (reordenamiento accidental dentro de un mismo grupo) que no se hubiera notado a simple vista con pocos viajes de prueba |

| 2026-08-24 | Agregar "Eliminar viaje" permanente, visible SOLO en viajes "Finalizado" (nunca "En curso"): en el detalle de viaje reemplaza a "Finalizar viaje" en la misma posición/estilo cuando el viaje ya está finalizado; modal de confirmación reutilizando `showConfirmModal()` con el nombre real del viaje y el número real de gastos; borrado en cascada real de la subcolección `expenses/` en Firestore antes del documento del viaje; redirect a Mis viajes tras borrar | Agregué `deleteTrip(tripId)` en `store.js`: borra en paralelo (`Promise.all`) cada gasto de `getExpensesByTrip(tripId)` (ya cargados en memoria desde `initStore`, mismo criterio de confiar en el estado ya sincronizado que usan `updateExpense`/`deleteExpense`) y recién después borra el documento del viaje — en ese orden explícito, no simultáneo, para no dejar la posibilidad de que el viaje desaparezca mientras algún gasto seguía sin borrarse. No hizo falta tocar `firestore.rules`: la regla existente (`allow read, write`) ya cubre `delete`, mismo caso que ya se confirmó al agregar `deleteExpense()`. En `trip-detail.js`, el botón nuevo (`#delete-trip`) ocupa exactamente la misma posición que `#close-trip` en el markup — son mutuamente excluyentes por el mismo `isClosed` que ya decidía cuál de los dos mostrar, así que nunca conviven los dos. Para el texto del modal pluralicé "gasto(s)"/"registrado(s)" a mano (`expenses.length === 1 ? "" : "s"`) en vez de dejar "1 gastos", que hubiera sido un detalle descuidado en un mensaje que ya de por sí advierte de una acción irreversible. No tuve que tocar `showConfirmModal()`: ya enfoca el botón "Cancelar" por defecto (no el de confirmar) desde que se escribió para el modal de presupuesto, así que el requisito de "que Enter no borre por accidente" ya estaba resuelto por el componente compartido, sin cambios. La redirección tras borrar usa `location.hash = "#/viajes"` (mismo patrón que ya usa el router para todas las navegaciones — dispara `hashchange`, que re-renderiza Mis viajes leyendo `getAllTrips()` ya sin el viaje borrado, sin recargar la página). Verifiqué con `vite build` que compilara, y visualmente con el mismo harness HTML estático de tareas anteriores (CSS real compilado): el botón "Eliminar viaje" en un viaje finalizado tiene el mismo estilo que "Finalizar viaje"/"Eliminar" de gastos, y el modal muestra el ícono de advertencia rojo, el nombre real del viaje y el conteo real de gastos, con el foco visible en "Cancelar" (no en el botón rojo) | Acotar la eliminación permanente a viajes ya finalizados (en vez de habilitarla también en activos, que no se pidió) es la misma decisión de reducir alcance que ya quedó documentada en `product-decisions.md`; la señalo también acá porque cambia directamente qué botón se ve en cada estado del viaje |

| 2026-08-24 | Agregar modales de éxito después de "Finalizar viaje" y "Eliminar viaje" en el detalle de viaje, reutilizando el mismo componente visual que "¿Eliminar este viaje?" (overlay + diálogo centrado, mismo tamaño/tipografía), cambiando solo ícono (check verde #A1D35B), texto y botón único "Aceptar" | Agregué `showSuccessModal({ title, onClose })` en `confirm-modal.js`, junto a `showConfirmModal()` ya existente, reusando literalmente las mismas clases (`.modal-overlay`, `.modal`, `.modal-title`, `.modal-actions`) en vez de un componente nuevo — solo el ícono cambia (`circle-check` en vez de `alert-octagon`) con un modificador `.modal-icon-success` que sobreescribe el color a `var(--color-accent)`. Usé `--color-accent` y no `--color-status-danger`'s contraparte `--color-status-ok` a propósito, aunque ambos tokens son el mismo hex `#a1d35b`: este check no es un estado del semáforo de presupuesto, es una confirmación de éxito genérica — mismo criterio de "no mezclar significados aunque compartan el hex" ya documentado para las píldoras de KPI por divisa. Como el modal de éxito no tiene `.modal-body` (solo un título, según el pedido), agregué `.modal-title-only` (`margin-bottom: var(--space-6)`) para que el espacio antes de los botones sea el mismo que ya daba `.modal-body` en el modal de confirmación — sin este ajuste el botón quedaba pegado al texto. El botón único usa `.button-primary` (mismo estilo que "Guardar gasto"/"Agregar gasto") en vez de `.button-danger`, porque ya no hay nada destructivo que remarcar en rojo en este paso. En `trip-detail.js`, encadené el modal de éxito directamente en el `await` de cada acción: para "Finalizar viaje", `onClose` dispara el mismo `render(container, { tripId })` que ya se usaba (recarga el detalle con el badge/botones ya actualizados a "Finalizado"); para "Eliminar viaje", `onClose` hace el `location.hash = "#/viajes"` que antes se ejecutaba inmediatamente después del `await deleteTrip()` — ahora se ejecuta solo al cerrar el modal de éxito, no antes, para que el usuario vea la confirmación antes de navegar. No agregué manejo de tecla Enter en `showSuccessModal()` (solo Escape, igual que `showConfirmModal()`): el foco ya cae en el único botón "Aceptar" al abrir, así que Enter ya lo activa por comportamiento nativo del `<button>` — agregar un handler de Enter en el `keydown` del documento hubiera disparado `close()` dos veces (una por el handler, otra por el click nativo del botón enfocado). Verifiqué con `vite build` que compilara, y visualmente con el mismo harness HTML estático de tareas anteriores (CSS real compilado): ambos modales de éxito muestran el check verde, el texto pedido exacto y el botón "Aceptar", con el mismo tamaño de diálogo que el modal rojo de al lado | Elegir `--color-accent` en vez de `--color-status-ok` para el verde de éxito es una decisión de significado, no solo de color — lo señalo porque los dos tokens son literalmente el mismo hex y el pedido solo especificaba el hex, no cuál de los dos tokens semánticos usar |

| 2026-08-24 | Cambiar el sidebar a fondo navy con el logo real en su versión "fondo oscuro" (assets SVG que el usuario subió a `public/img/`: Logotipo/Isotipo × fondo-claro/fondo-oscuro), franja verde en el item activo en vez de fondo, texto/íconos en blanco atenuado para inactivos con hover sutil | Reemplacé el logo CSS-puro (`.logo-mark`/`.logo-wordmark` en `sidebar.js`) por dos `<img>` reales: `Logotipo-fondo-oscuro.svg` completo en desktop y `Isotipo-fondo-oscuro.svg` (solo el ícono) en mobile, alternados por CSS con el mismo patrón de breakpoint que ya ocultaba el wordmark antes — así el sidebar mobile colapsado sigue mostrando solo el ícono, ahora con el asset real en vez del monograma "T" hecho con `::before`. Para el item activo, en vez de `background: var(--color-primary)` (que ya no contrasta sobre el sidebar navy) usé un `border-left: 4px solid transparent` en TODOS los items (activo e inactivo) y solo coloreo ese borde a verde (`--color-accent`) en `.is-active` — reservar el espacio del borde en el estado base evita que el ícono/texto salte 4px al activarse o desactivarse un item. Agregué `border-top-left-radius: 0`/`border-bottom-left-radius: 0` solo al item activo para que la franja se vea perfectamente recta contra el borde izquierdo, sin la curvatura del `border-radius` que sí tienen los inactivos. Para el texto/ícono inactivo usé `rgba(255, 255, 255, 0.75)` (no un token nuevo) y blanco puro (`--color-surface`) + `font-weight: 600` para el activo — la diferencia de opacidad ya es suficiente para distinguir "más brillante" sin necesitar un segundo verde. El hover usa `rgba(255, 255, 255, 0.08)` como overlay (mismo criterio de "navy un poco más claro" que pedía el enunciado, sin necesitar un token de navy-claro nuevo) — visualmente distinto de la franja verde del activo, así que no se confunden aunque el usuario pase el mouse por un item activo. Actualicé `docs/branding.md` (sección Logo y Aplicación) para reflejar que el logo ya no es CSS puro sino los 4 archivos SVG reales. Verifiqué con `vite build` que compilara y que los SVG de `public/img/` se copiaran a `dist/img/`, y visualmente con Playwright (viewport desktop 1000px, mobile 420px, y un `hover()` real sobre un item inactivo) usando el CSS y los assets reales — confirmé el logo nítido en ambos tamaños, la franja recta en el item activo, el hover sutil distinguible de la franja, y el colapso a isotipo-solo en mobile | El favicon (isotipo fondo claro) y el logo de la futura LP quedaron fuera de este cambio a propósito — el pedido de esta tarea era solo el sidebar; el usuario ya adelantó la intención para esos dos casos en la conversación, pendiente de que lo pida explícitamente |

| 2026-08-24 | En Detalle de viaje, envolver el estado vacío de "Gastos registrados" (mensaje "Todavía no has registrado gastos...") en una tarjeta blanca, con el mismo radius/padding/sombra que las demás tarjetas de la pantalla — antes era texto suelto sin tarjeta, inconsistente con los KPIs y la torre de categorías al lado. El título se queda fuera, como texto normal | Agregué `.expense-list-empty` en `base.css` reusando los mismos TOKENS que ya usan `.kpi-card`/`.bar-chart` (`var(--radius-md)`, `var(--color-surface)`, `var(--shadow-sm)`) en vez de un valor hardcodeado nuevo — no existe una clase `.card` genérica compartida en el proyecto (cada componente-tarjeta define su propio bloque con los mismos tokens), así que seguí ese mismo patrón ya establecido en vez de introducir una abstracción nueva para un solo uso. Envolví el `<p class="empty-state">` existente en un `<div class="expense-list-empty">` en `trip-detail.js` sin tocar la clase `.empty-state` en sí (se reusa en otras 6+ pantallas para estados vacíos sin tarjeta — Mis viajes, Alertas, etc. — cambiarla directamente hubiera afectado todas esas otras pantallas, que el pedido explícitamente no pedía tocar). El centrado usa `display:flex; justify-content:center` en el wrapper (el `.empty-state` interno ya es un flex item de ancho de contenido, así que se centra solo) y `padding: var(--space-8) var(--space-4)` para el aire vertical extra pedido. El estado CON gastos (`<ul class="expense-list">`) no se tocó. Verifiqué con `vite build` que compilara, y visualmente con un harness HTML estático (CSS real compilado) que la tarjeta se ve idéntica en estilo a las demás tarjetas de la pantalla, con el mensaje centrado y sin verse apretado | No había una clase de tarjeta genérica que reusar literalmente — reusar los tokens en vez de una clase compartida es la interpretación más fiel de "no reutilices valores sueltos" dado el patrón ya existente en el resto del codebase |

| 2026-08-24 | Convertir el formulario de "Agregar gasto"/"Editar gasto" (antes un bloque inline en el flujo de la página) en un modal, reutilizando el mismo overlay/tarjeta que "¿Eliminar este viaje?" (`.modal-overlay`/`.modal`, mismo radius y sombra) | A diferencia de `showConfirmModal()`/`showSuccessModal()` (que crean y destruyen su overlay con JS en cada apertura), este modal necesitaba bindings de formulario (submit, precarga en modo edición, mensaje de error inline) que ya vivían en el `render()` de `trip-detail.js` — en vez de reescribir todo ese flujo como un componente nuevo tipo `showExpenseModal()`, dejé el `<div class="modal-overlay" id="expense-modal" hidden>` embebido en el propio markup de la página (igual que ya vivía el `<form>` antes) y solo cambié CÓMO se abre/cierra: el atributo `hidden` en vez de `form.hidden` controlando el `<form>` directamente. Al probar esto con Playwright (harness HTML con el CSS real, clicks reales, no solo capturas estáticas) encontré un bug real antes de darlo por bueno: `.modal-overlay` ya tenía `display:flex` en su propia regla, que EMPATA en especificidad con la regla `[hidden]{display:none}` del navegador — y como el CSS del autor siempre gana sobre el del user-agent en un empate, el modal quedaba visible desde el primer render aunque tuviera `hidden`, bloqueando clics sobre toda la pantalla. `showConfirmModal()`/`showSuccessModal()` nunca lo sufrieron porque ELLOS ni siquiera dejan el overlay en el DOM mientras está cerrado (lo crean/destruyen con JS), así que nunca dependieron de `[hidden]`. Lo arreglé agregando `.modal-overlay[hidden] { display: none; }` en `base.css` — mayor especificidad que la regla base, protege también cualquier modal embebido futuro que use este mismo patrón. Reescribí `exitEditMode()`/`enterEditMode()` como `closeExpenseModal()`/`openAddModal()`/`openEditModal()`: el botón "Cancelar" ya no se oculta en modo "agregar" (antes solo aparecía en edición) porque ahora es la forma principal de cerrar el modal sin guardar en AMBOS modos, tal como pedía el punto 3. El botón "+ Agregar gasto" dejó de comportarse como acordeón (antes un segundo click lo cerraba) y ahora solo abre — cerrar quedó exclusivamente en manos de "Cancelar" y el click en el overlay, que es el patrón esperado de un modal. No agregué cierre con Escape: a diferencia de `showConfirmModal()` (que puede agregar/quitar su propio listener de `document` porque crea y destruye el overlay entero), este modal vive en el DOM permanente de la página — un listener de `document` atado a su ciclo de apertura/cierre sería fácil de dejar huérfano en alguno de los varios re-renders completos que dispara esta pantalla (guardar gasto, eliminar gasto, finalizar viaje, etc.), y el pedido tampoco lo pedía explícitamente (solo "Cancelar" y "click fuera"). El resto de la pantalla no se mueve mientras el modal está abierto porque `.modal-overlay` ya es `position:fixed` — viene gratis de reusar el mismo componente, no necesitó ningún ajuste de layout aparte. Verifiqué con `vite build` que compilara, y con Playwright contra un harness HTML que reproduce el markup y los bindings reales 1:1 (mismo CSS compilado): confirmé por script (no solo visualmente) que el modal arranca cerrado, abre con el título correcto en cada modo, "Cancelar" cierra y limpia el campo sin dejar el monto tipeado, el click en el overlay también cierra, "Editar gasto" precarga el monto/categoría reales, y el `getBoundingClientRect()` de los KPIs es idéntico antes/después de abrir y cerrar el modal (no hay reflow) | El bug de `[hidden]` pisado por `display:flex` no se hubiera detectado con una captura estática sola (el timeout de Playwright al intentar clickear un elemento "tapado" fue la señal) — confirma el valor de probar clicks reales, no solo comparar screenshots, cuando el cambio es de comportamiento interactivo y no solo visual |

| 2026-08-24 | Reemplazar el header mobile (íconos de nav sueltos + chip de cuenta expuesto) por un menú hamburguesa único: barra mobile con solo hamburguesa+logo, drawer deslizable desde la izquierda (mismo fondo navy y estilo de item que el sidebar desktop, franja verde incluida) con nav arriba y sesión (foto+nombre+email+cerrar sesión) al fondo; y en Inicio mobile, el buscador colapsa a un ícono de lupa junto a "Nuevo viaje" que expande el input a ancho completo debajo del header | Usé el breakpoint ya existente del proyecto (`@media (max-width: 45rem)`, 720px) en vez del "~768px" que mencionaba el enunciado como aproximación — el pedido decía explícitamente "breakpoint existente del proyecto", y agregar uno nuevo hubiera creado un rango raro donde el sidebar colapsa en un punto y el drawer en otro. Extraje `renderNavItems(currentPath)` en `sidebar.js` para generarlos una sola vez y reusarlos tal cual en la barra desktop Y en el drawer — ambos quedan con literalmente la misma clase `.sidebar-nav-item` (franja verde del activo incluida) sin duplicar el criterio de qué item está activo. El bloque de sesión del drawer es una versión nueva, no reusa `components/account-menu.js` (que sigue siendo el dropdown de desktop, sin tocar): a diferencia del dropdown, acá todo va siempre visible sin colapsar (foto+nombre+email+botón de cerrar sesión en una sola fila), porque el drawer ya es la superficie que el usuario abrió a propósito — un segundo nivel de colapso ahí sería fricción extra. Como `renderSidebar()` no recibía datos de usuario, hice que `sidebar.js` llame a `getCurrentUser()` directamente (mismo patrón síncrono que ya usa cada página) en vez de tener que pasarlo a través de `router.js`/`app-shell.js`. Agregué `bindSidebar()` (antes `sidebar.js` no exportaba ningún bind, no tenía JS interactivo) y lo até desde `renderShell()` en `app-shell.js`, una sola vez por navegación — no en cada página — porque el drawer es del shell, no de una pantalla en particular. La animación de deslizamiento (`transform: translateX`) necesitó un detalle: si solo alterno `hidden`, no hay transición posible (display:none no anima) — así que al abrir quito `hidden` y agrego la clase `.is-open` en el frame SIGUIENTE (`requestAnimationFrame`) para que el navegador alcance a pintar el estado "cerrado" antes de animar a "abierto"; al cerrar, quito `.is-open` pero recién pongo `hidden` de vuelta cuando termina la transición (evento `transitionend`), para no cortar la animación de salida a medio camino. Antes de escribir la CSS del overlay recordé el bug real que encontré la tarea anterior con `.modal-overlay`/`[hidden]` (display:flex del autor le gana a `[hidden]{display:none}` del navegador en un empate de especificidad) y agregué la misma regla de seguridad (`.drawer-overlay[hidden] { display: none; }`) desde el principio, además de un `display: none` incondicional en `.drawer-overlay` fuera de cualquier media query — así el drawer es literalmente imposible de mostrar en desktop, lo confirmé forzando `hidden = false` y la clase `.is-open` por JS directamente en un test contra la versión desktop y verificando que igual siguiera en `display: none`. Para el buscador de Inicio, en vez de mover el `<input>` a otro lugar del DOM (que hubiera obligado a re-bindear o duplicar el elemento con el mismo id) dejé el mismo `.search-field` de siempre y solo le agrego `flex: 1 1 100%; order: 10;` cuando está expandido — como `.page-header-actions` ya tenía `flex-wrap: wrap` de antes, un item que pide 100% de ancho cae solo a su propia fila, empujando el resto de la página hacia abajo sin necesitar `position:absolute` ni JS de layout. Todo el CSS nuevo del buscador quedó scoped a `.page-home` explícitamente porque Mis viajes reusa el mismo `components/search-field.js` y el pedido aclaraba que este comportamiento era solo para Inicio. Agregué los íconos `menu`/`x` de Lucide (no existían en `icons.js`). Verifiqué con `vite build` que compilara, y con Playwright contra un harness HTML que reproduce el markup y los bindings reales 1:1 (mismo CSS compilado), en dos viewports (mobile 390px, desktop 1200px): confirmé que en mobile el header queda solo con hamburguesa+logo (sin nav ni chip de cuenta), que el drawer abre con la franja verde en "Inicio" y el bloque de sesión con los datos reales, que se cierra con el click en un item de nav, con el click en el overlay y con la X, y que el buscador expandido empuja el KPI de abajo (comparé `getBoundingClientRect().top` antes/después); y que en desktop el sidebar/header quedan exactamente iguales a antes (hamburguesa y buscador-ícono ocultos, chip de cuenta y buscador inline visibles, drawer en `display:none` inclusive forzándolo por JS) | El bug de `.modal-overlay[hidden]` de la tarea anterior directamente informó el diseño de `.drawer-overlay` desde el primer intento (no tuve que redescubrirlo) — vale la pena tenerlo presente para cualquier futuro overlay embebido en el propio markup de una página en vez de creado/destruido con JS |

| 2026-08-24 | Ajuste al header mobile de la tarea anterior: logotipo completo (isotipo+"TripFlow", ya no el isotipo solo) alineado a la izquierda; a la derecha, en este orden, lupa (buscar) y hamburguesa, esta última la más a la derecha del todo; la lupa expande un input a ancho completo debajo del header (empuja el contenido, no superpone), se cierra tocándola de nuevo o con una X dentro del input | Antes de tocar código le pregunté al usuario cómo resolver una ambigüedad real del pedido: pedía la lupa "en el header" junto a la hamburguesa, pero la hamburguesa vive en `sidebar.js` (barra compartida por las 4 pantallas), mientras que el buscador de la tarea anterior solo existía en Inicio, como una fila aparte de esa barra — mover la lupa tal cual a la barra compartida la hubiera puesto también en Alertas/Detalle de viaje, que no tienen nada que buscar. El usuario contestó explícitamente: la lupa debe verse en el header mobile de Inicio Y Mis viajes (las dos pantallas con lista de viajes buscable, igual que en desktop), pero NO en Alertas ni Detalle de viaje. Implementé esto moviendo el buscador entero (ícono + fila expandible) de `dashboard.js` a `sidebar.js`, condicionado a `SEARCHABLE_PATHS = new Set(["/", "/viajes"])` evaluado sobre el mismo `currentPath` que ya recibía `renderSidebar()` — no hizo falta ninguna configuración cruzada entre página y shell, alcanzó con el path de la ruta actual. El input mobile (`#trip-search-mobile`) vive en el DOM del shell (fuera de `container`, el nodo que cada página recibe para renderizar), así que `dashboard.js`/`my-trips.js` ahora lo buscan con `document.querySelector` en vez de `container.querySelector`, y bindean el MISMO handler de filtro al input de siempre (desktop, sin tocar) y a este nuevo — en `my-trips.js` tuve que sacar `update()` de leer `searchInput.value` directamente (ya no hay un único input) y guardar el query en una variable (`currentQuery`), porque `update()` también se llama desde el click de pestaña, sin ningún evento de por medio. En `sidebar.js`, "colapsar y volver al estado original" (pedido explícito del punto 3) lo implementé limpiando el `value` del input Y disparando un evento `input` sintético al cerrar (por la lupa o por la X) — así el filtro de la página, bindeado aparte, recalcula solo la lista sin filtro, sin necesitar que `sidebar.js` conozca nada de cómo cada página filtra. Repetí el ajuste de logo (quité el swap a isotipo compacto en mobile: ahora `.sidebar-logo-full` se muestra siempre, mismo alto fijo/ancho automático, sin recortarse) y dejé `.sidebar-logo-compact`/su `<img>` fuera del markup — sin ningún uso ya, no lo dejé como código muerto. Encontré y corregí un bug real al probar con Playwright antes de dar la tarea por cerrada: había puesto la regla `.mobile-search-row { display: none; }` incondicional DESPUÉS del `@media (max-width: 45rem)` que la vuelve `display: flex` — mismo empate de especificidad de bugs anteriores (`.modal-overlay[hidden]`, `.drawer-overlay[hidden]`), pero esta vez el problema no era el atributo `[hidden]` sino el ORDEN en el archivo: con la misma especificidad, la regla que aparece último en el CSS gana, y la mía "incondicional" quedaba físicamente después de la del media query, ganándole siempre y dejando la fila invisible aunque se quitara `hidden`. Lo detecté porque Playwright tiró timeout intentando escribir en un input "no visible" — la reordené (la unconditional ahora antes del media query, mismo lugar que ya usan `.drawer-overlay`/`.modal-overlay`) y quedó consistente con el resto de overlays de la app. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el CSS real, dos páginas: Inicio y Alertas) que el orden visual de los 3 elementos del header es el pedido (comparé `getBoundingClientRect().left` de logo/lupa/hamburguesa), que la lupa filtra la lista real a través del input mobile, que cierra y limpia con la X y volviendo a tocar la lupa, y que en Alertas ni el ícono ni la fila existen en el DOM (no solo ocultos) mientras la hamburguesa sigue funcionando igual | El error de orden en el archivo CSS es la misma clase de bug que los dos anteriores (empate de especificidad) pero con una causa distinta (orden de aparición, no el atributo `[hidden]`) — lo anoto porque el patrón "regla incondicional antes del media query que la sobreescribe" ya es la convención establecida en esta base de código (`.drawer-overlay`, `.modal-overlay`) y esta vez la rompí sin querer al escribir las reglas en el orden "que se leía mejor" en vez de en el orden que la cascada necesita |

| 2026-08-24 | Corregir la superposición de texto en mobile de cada fila de "Gastos registrados" (Detalle de viaje): en vez de la fila horizontal de desktop, apilar en 5 líneas (ícono+categoría / nota / monto / fecha / acciones) | Resolví esto 100% en CSS, sin tocar `trip-detail.js` ni el markup de `renderExpenseItem()`: en mobile, `.expense-item` pasa de `display:flex` a `display:grid` con `grid-template-areas` (`icon category` / `. note` / `. amount` / `. date` / `. actions`), y los dos contenedores intermedios que agrupan categoría+nota (`.expense-item-body`) y monto+fecha (`.expense-item-meta`) pasan a `display:contents` — eso los "desaparece" de la caja del grid sin tocar el DOM, dejando que sus hijos (categoría, nota, monto, fecha) participen cada uno como celda propia del grid del `<li>`, en vez de quedar atrapados como un solo bloque cada uno. El monto (`.expense-item-amount`) queda alineado a la izquierda en mobile poniendo `text-align:left` en `.expense-item-meta` (el `display:contents` no rompe la herencia de CSS, que sigue el árbol del DOM, no la caja de layout) — así no hizo falta repetir la regla en monto y fecha por separado. Elegí `display:contents` en vez de la alternativa obvia (aplanar el HTML quitando los `<div>` intermedios) porque el pedido explícitamente decía "no modificar la lógica... solo el layout visual", y esta técnica logra exactamente eso: cero cambios en JS, todo resuelto en `base.css`, dentro del media query existente. No toqué `.expense-list`'s `gap` (separación entre cards, ya "usado en desktop" según el propio pedido) porque no está scopeado por breakpoint — cambiarlo hubiera afectado también desktop, que el pedido pedía dejar intacto. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el markup real de `renderExpenseItem()`, CSS real compilado, 3 gastos con/sin nota y con nota larga) en mobile (390px) y desktop (1000px): confirmé por script que `.expense-item` es `display:grid` en mobile y `display:flex` en desktop (breakpoint aplicándose donde corresponde) y que el `getBoundingClientRect()` de la categoría no se superpone con el del monto (`amount.top >= category.bottom`) — no solo a ojo, sino midiendo las cajas reales | `display:contents` es la pieza que hizo posible resolver esto sin tocar el markup — vale la pena recordarla para el próximo caso de "necesito reagrupar visualmente hijos de contenedores distintos sin duplicar ni aplanar el HTML" |

| 2026-08-24 | Corregir la superposición del header de Detalle de viaje en mobile: foto+nombre+badge en su propia línea arriba, y "Agregar a Google Calendar"/"Compartir por WhatsApp"/"Agregar gasto" como fila de acciones debajo — "Agregar gasto" primero y más ancho, los otros 2 a solo ícono | Mismo criterio que la fila de búsqueda de Inicio (tarea anterior): `.page-header` (ya `display:flex`, sin wrap) pasa a `flex-wrap:wrap` SOLO para `.page-trip-detail` (scoped, no toca el header de las otras 3 pantallas), y `.trip-detail-title` pide `flex-basis:100%` — al no caber nada más en esa línea, `.page-header-actions` cae solo a la fila de abajo, sin mover ni un pixel de layout en desktop (la regla vive dentro del media query). Para el orden visual pedido (Agregar gasto primero, aunque en el DOM sigue último — no lo reordené en el markup para no arriesgar nada de la lógica de submit/toggle que ya cuelga de ese mismo botón) usé `order:-1` en `#toggle-expense-form` y `flex:1` para que sea el más ancho, tal como pedía el punto (a). Calendar/WhatsApp pasan a solo ícono ocultando su `<span>` (mismo patrón ya usado para `.sidebar-nav-item span` en mobile) y ajustando el padding a cuadrado (`var(--space-2)` en las 4 direcciones, en vez del rectangular con `--space-4` horizontal que tenían con texto) — sin tocar `.button-outline` en sí (que sigue con el mismo borde en el resto de la app), solo un override scoped a esta fila. Agregué `aria-label`/`title` a los 2 links en `trip-detail.js` (no existían antes, el texto visible ya cumplía de accesible name) porque al ocultar el `<span>` con `display:none` ese texto deja de estar disponible para lectores de pantalla — el `title` además da el tooltip nativo al mantener presionado/hacer hover que pedía el enunciado, sin JS adicional. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el markup real del header, un nombre de viaje largo para probar el truncamiento a la vez) en mobile (390px) y desktop (1100px): confirmé por script que título y acciones no se superponen (`actions.top >= title.bottom`), que "Agregar gasto" es el más ancho de los 3 y aparece primero de izquierda a derecha, que el texto de calendar/whatsapp queda oculto (`display:none`) con su `title` presente, y que en desktop la fila sigue en una sola línea (`flex-wrap:nowrap`) con el texto de los 3 botones visible | Reusar `order`/`flex-basis:100%` (en vez de reordenar el HTML) para expresar un cambio puramente visual es el mismo criterio que ya se aplicó 2 tareas atrás para el buscador de Inicio — lo mantengo consistente en toda la app en vez de resolver cada caso de reordenamiento mobile con una técnica distinta |

| 2026-08-24 | 5 ajustes en Inicio/Mis viajes (mobile) + logo clickeable (ambas plataformas): (1) confirmar que el buscador inline queda oculto en mobile (ya implementado, pero encontré que estaba roto — ver más abajo); (2) logo del sidebar/header como link a Inicio; (3) "Hola, Steven"/"Mis viajes" en una sola línea en mobile; (4) FAB circular de "Nuevo viaje" en Inicio mobile, reemplazando al botón del header; (5) dona de "Gasto por categoría" en columna (dona arriba a ancho completo, leyenda debajo) en mobile | Antes de tocar nada verifiqué el punto 1 con Playwright, dado por hecho como "ya implementado" — y encontré que estaba roto: `.search-field { display: none; }` (agregado en la tarea del header mobile) quedó ANTES en el archivo que la regla base `.search-field { display: flex; ...}` (que vive más abajo, junto a `.search-field-icon`/`.search-field input`) — mismo empate de especificidad que ya causó bugs en `.mobile-search-row` y `.modal-overlay` en tareas anteriores, esta vez pasó desapercibido porque ningún test previo verificó específicamente que el buscador INLINE (no el de la fila expandible) quedara oculto. Lo corregí moviendo la regla `display:none` a un media query nuevo justo después de la definición base, mismo patrón ya establecido. Para el logo clickeable, cambié `.sidebar-logo` de `<div>` a `<a href="#/">` en `sidebar.js` — como ese markup ya es compartido entre desktop y mobile (mismo elemento, sin duplicar), cubre ambas plataformas con un solo cambio; agregué `cursor:pointer`/`text-decoration:none` explícitos aunque los links ya los traen por defecto, porque el enunciado los pedía explícitamente. Para que los títulos no partan en 2 líneas agregué `font-size: var(--text-xl)` (un paso por debajo de `--text-2xl`, sigue siendo el título más grande de la pantalla) + `white-space:nowrap` a `.page-header h1` dentro del media query — sin scopear por página: confirmé que no afecta el `<h1>` de Detalle de viaje porque ese vive dentro de `.trip-detail-title h1`, una regla más específica que ya le gana a esta. El FAB reusa la misma ruta `#/nuevo-viaje` que el botón de header, oculta este último solo en `.page-home` mobile (`.page-home .page-header-actions .button-primary`, scoped para no tocar "Agregar gasto" de Detalle de viaje, que también es `.button-primary`) y usa `env(safe-area-inset-bottom, 0px)` para el margen del gesto/barra de sistema en iOS — `index.html` ya tenía `viewport-fit=cover` desde antes, no hizo falta agregarlo. Para la dona, en vez de tocar `donut-chart.js` (el SVG ya usa `viewBox`, no depende de sus atributos `width`/`height` fijos de 160px para verse nítido) escalé el contenedor `.donut-chart-visual` a `width:100%` + `aspect-ratio:1/1` y el `<svg>` a `width:100%;height:100%` — el navegador reescala el `viewBox` proporcionalmente sin recorte ni deformación, y `.donut-chart-total` sigue centrado porque sigue siendo `inset:0` sobre el mismo contenedor, ahora más grande; no toqué el `max-width:5.5rem` del monto central (documentado para topar el hueco de 96px del tamaño original) porque no causa ningún bug al quedar chico dentro de un hueco más grande, solo deja de aprovechar todo el espacio extra, y tocarlo estaba fuera de lo pedido. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el markup real de Inicio y Mis viajes, CSS real compilado) en mobile (390px) y desktop (1200px): 17 checks — buscador inline oculto en ambas pantallas mobile, logo es un `<a href="#/">` con cursor pointer en desktop, los 2 títulos miden menos de 40px de alto (una sola línea), el botón de header queda oculto y el FAB visible/fixed en mobile (e inverso en desktop), y la dona queda en columna con el diámetro mayor a 200px en mobile (vs. los 160px fijos de desktop) y en fila en desktop | El bug del buscador inline no visible-pero-roto es la tercera vez que aparece esta misma clase de error (regla incondicional después de la que la sobreescribe, incluso con el mismo criterio ya documentado 2 veces) — a partir de acá, cada vez que agregue una regla `display:none` dentro de un media query para "apagar" un elemento que ya tiene su propia regla base en el archivo, voy a verificar explícitamente en qué ORDEN quedan las dos antes de dar la tarea por cerrada, no solo confiar en que la especificidad alcanza |

| 2026-08-24 | Dos ajustes en Inicio: (1) FAB "Nuevo viaje" (mobile) pasa de navy a verde (`--color-accent`) y crece ~21% (3.5rem → 4.25rem); (2) eliminar por completo el bloque "Gasto vs presupuesto" (mobile y desktop) por redundante con el KPI "Presupuesto restante" ya existente, expandiendo "Gasto por categoría" a todo el ancho | Antes de cambiar el color del FAB calculé el contraste real de blanco vs. navy sobre `#A1D35B`: blanco da ~1.75:1 (falla incluso el mínimo de texto grande, 3:1), navy da ~9:1 — cambié el ícono a navy, no blanco, con el cálculo como justificación en el propio comentario del CSS, no solo "se ve mejor". Antes de borrar `components/budget-ring.js` confirmé con `grep` que `dashboard.js` era su único importador en toda la app — el comentario de `trip-detail.js` que lo mencionaba (para explicar por qué `STATUS_SHARE_LABELS` usa esas 3 frases exactas) y el de `utils/currency.js` (ejemplos de uso de `splitCurrencyUnit`) eran referencias en comentarios, no imports reales, así que los actualicé para no dejar código apuntando a un archivo borrado, sin tocar la lógica que describen. Al quitar `<div class="dashboard-charts-grid">` (ya redundante con un solo hijo) también borré su CSS — grid 50/50 que ya no tiene sentido con una sola sección — y `.budget-ring-*` completo (11 reglas). `computeKpis()` en `dashboard.js` dejó de exponer `totalBudget` en el objeto que devuelve (ya no lo consume nadie fuera de la propia función, donde sigue viviendo como variable local para calcular `remaining`) en vez de dejarlo como campo muerto en el valor de retorno. Actualicé también `docs/design-system.md` (quité la fila de "Anillo de progreso", agregué una fila para el FAB) y `docs/branding.md` (la sección de sidebar todavía decía "en mobile colapsa al isotipo solo", desactualizada desde la tarea del logo clickeable — la corregí de paso, ya que estaba tocando esa misma sección). Verifiqué con `vite build` que compilara sin el import roto, y con Playwright (harness HTML con el markup real, CSS compilado) en mobile (390px) y desktop (1200px): confirmé el color exacto del FAB por `getComputedStyle` (`rgb(161, 211, 91)` = `#A1D35B`, ícono `rgb(0, 24, 96)` = `#001860`), el nuevo ancho (~68px = 4.25rem, contra los ~56px anteriores), que `.budget-ring-card` ya no existe en el DOM en ninguna de las 2 plataformas, y que la dona ocupa más del 90% del ancho del `<main>` en desktop (antes competía 50/50) | Corregir las 3 referencias muertas al componente borrado (2 comentarios + 1 fila de tabla en design-system.md) en el mismo commit, no en uno aparte, porque son parte de la misma limpieza — dejarlas hubiera sido código/documentación que describe algo que ya no existe |

| 2026-08-24 | Unificar "Gasto por categoría" por BREAKPOINT en vez de por pantalla: torres en desktop, dona en mobile, tanto en Inicio (antes dona fija en las 2 plataformas) como en Detalle de viaje (antes torres fijas en las 2) | Extraje `components/category-chart.js`, un componente nuevo y chico que envuelve los 2 componentes YA existentes (`barChart()`/`donutChart()`) sin tocar ninguno de los dos — cada uno sigue calculando/renderizando exactamente igual que antes, `category-chart.js` solo los pone juntos en el DOM (`.category-chart-bars`/`.category-chart-donut`, cada uno con su propia tarjeta ya estilada) y el CSS decide cuál se ve según el ancho, mismo criterio que ya usan `.sidebar-logo-full`/`-compact` y `.search-field`/`.mobile-search-row` en tareas anteriores — no hubo que evaluar "es razonable el refactor": ya existía la infraestructura para este patrón, extraer el componente fue más simple que duplicar el markup en las 2 páginas. `donutChart()` y `barChart()` ya esperaban prácticamente la misma forma de dato (`{id, label, icon, value, color}`, donut solo ignora `id`/`icon`) porque `trip-detail.js`'s `computeCategoryBreakdown()` ya la traía completa desde antes (la necesitaba para las torres) — el único cambio de datos real fue agregarle `id`/`icon` al `computeCategoryBreakdown()` de `dashboard.js`, que antes solo devolvía `{label, value, color}` porque la dona no los necesitaba; ningún cálculo de %/monto se tocó en ninguna de las 2 funciones. Actualicé las 2 llamadas (`dashboard.js`, `trip-detail.js`) de `donutChart(...)`/`barChart(...)` a `categoryChart(...)`, sin tocar el resto de cada página. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el CSS real compilado, mismas 6 categorías con 3 en $0 para probar el estado gris de las torres) en mobile (390px) y desktop (900px): confirmé por `getComputedStyle` que `.category-chart-bars` queda oculto y `.category-chart-donut` visible en mobile, y al revés en desktop, y visualmente que ambos formatos se ven exactamente como ya se veían en sus pantallas originales (torres con el monto arriba/ícono+nombre debajo/gris en $0; dona con el total centrado y la leyenda con punto+nombre+%·monto debajo) | Escribí explícitamente en el propio comentario de cabecera de `category-chart.js` los otros 2 pares ya existentes en la app que usan el mismo patrón "ambos formatos en el DOM, CSS decide cuál se ve" — no para que quede bonito, sino para que quede documentado que este NO es un patrón nuevo inventado para esta tarea, ya estaba probado 2 veces antes |

| 2026-08-24 | Agregar un badge de notificación (círculo rojo con el número de alertas activas, "9+" si son 10 o más, oculto si son 0) sobre el ícono de campana ("Alertas") en el menú de navegación, en sidebar desktop y drawer mobile por igual | El pedido explícito era "reutilizar esa lógica/estado, no duplicar el cálculo" — `alerts.js` ya calculaba la lista de viajes en alerta inline dentro de su propio `render()` (mapear viajes activos a `{trip, status}`, filtrar `status !== OK`), así que extraje esa parte (sin el `.sort()`, que es específico de cómo Alertas ordena su lista, danger-primero) a `getTripAlerts()` en `data/store.js` — única fuente para ambos consumidores: `alerts.js` ahora solo le agrega el `.sort()` encima, y `sidebar.js` usa `.length` del mismo resultado para el badge. Antes de mover el código confirmé que `utils/status.js` no importa nada de `data/store.js` (sin riesgo de import circular al agregarle `getBudgetStatus`/`STATUS` a store.js). En `sidebar.js`, el wrapper `<span class="sidebar-nav-icon">` (necesario para anclar el badge con `position:absolute` en su esquina) solo envuelve el ícono de "Alertas" — los otros 2 items (Inicio, Mis viajes) siguen renderizando el ícono suelto, exactamente como antes, para cumplir literalmente "no modificar el resto de ítems del menú" sin arriesgar su layout por un cambio que no les aplica. `renderNavItems(currentPath)` se sigue llamando 2 veces (barra + drawer, mismo criterio ya establecido para no duplicar el markup de nav) — cada llamada recalcula `getTripAlerts().length` de forma independiente y barata (recorre viajes activos en memoria, sin red), así que el badge sale ya actualizado en las 2 superficies con cada render del shell, que ocurre en cada navegación (`app-shell.js`) — cubre el caso "el usuario resuelve una alerta y vuelve a Inicio" sin necesitar ningún mecanismo de suscripción/estado reactivo nuevo, porque la navegación ya dispara un re-render completo del shell. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el CSS real compilado) con 3 escenarios (0, 5 y 12 alertas): confirmé que con 0 el badge no existe en el DOM (no solo oculto), que con 5 muestra "5", que con 12 muestra "9+", y que los otros 2 items del menú no tienen el wrapper nuevo | Extraer `getTripAlerts()` a `store.js` en vez de duplicar el filtro en `sidebar.js` es la única forma real de cumplir "no duplicar el cálculo" que pedía el enunciado — un segundo `filter(status !== OK)` en otro archivo hubiera sido exactamente el tipo de duplicación que se pidió evitar, aunque el resultado visual fuera idéntico |

| 2026-08-24 | Reemplazar 2 de los 3 KPIs de Inicio ("Gastado en el periodo", "Presupuesto restante") por "Viajes en riesgo", reutilizando exactamente el mismo conteo que ya usa el badge de la campana de "Alertas" — grid de 3 columnas a 2 | El pedido era explícito: "no dupliques la lógica, reutiliza el mismo valor/estado" — como `getTripAlerts()` ya existía en `data/store.js` (extraída para el badge de la campana la tarea anterior), acá solo hizo falta `.length` sobre el mismo resultado, sin ningún cálculo nuevo. Al quitar los 2 KPIs de dinero, `computeKpis()` en `dashboard.js` quedó reducida a devolver un solo campo trivial (`activeCount: trips.length`) — la borré por completo y usé `trips.length` directo en el render, en vez de mantener una función de una sola línea sin razón de ser; eso dejó `getTripTotal`/`formatCurrency` sin ningún otro uso en el archivo (confirmé con lectura completa, no solo grep del nombre) y los quité de los imports. Para el ícono usé `alert-triangle` (el nombre literal que sugería el enunciado, "alerta/triángulo") en vez de `alert-octagon` (el que realmente usan las cards en estado "Límite superado" específico) — como el KPI agrega AMBOS estados de alerta (warning y danger, no solo danger), el triángulo genérico de advertencia representa mejor "esto necesita atención" que el octágono, reservado en el resto de la app específicamente para "ya se pasó del límite". Para el grid, probé primero sin tocar CSS (el `auto-fit` compartido de `.kpi-row` ya colapsa solo a 1 columna en mobile) y con Playwright a 1100px de ancho vi que las 2 cards se estiraban a ~530px cada una con mucho espacio vacío adentro — nada roto, pero se veía peor que las 3 originales. Agregué `.page-home .kpi-row { grid-template-columns: repeat(2, minmax(12rem, 20rem)); }` dentro de un `@media (min-width: 45rem)` (no toca mobile, que ya se veía bien) y scoped a `.page-home` (no la regla compartida, que Mis viajes con 4 KPIs y el detalle de viaje con 3 en columna siguen necesitando tal cual). Verifiqué con `vite build` que compilara sin los imports muertos, y con Playwright (harness HTML con el CSS real compilado) que en desktop (1100px) las 2 cards miden exactamente 320px (el tope de 20rem) en vez de estirarse, y que en mobile (390px) siguen apiladas a ancho completo, igual que antes | Reusar `getTripAlerts()` en vez de reimplementar el filtro fue automático esta vez porque ya existía de la tarea anterior — quedó documentado en `product-decisions.md` que esa decisión de extraerlo a una función compartida fue justamente para casos como este, donde un segundo consumidor necesita el mismo número |

| 2026-08-24 | Agregar una tarjeta "Próximas actualizaciones" (informativa, no clickeable) en el espacio vacío bajo la nav del sidebar desktop y del drawer mobile — mismas 3 líneas del roadmap ya documentado en README.md/product-decisions.md — con fondo navy más oscuro y título verde chico | El requisito de "fondo 15-20% más oscuro... usando una variable nueva o un color derivado del token navy existente" lo resolví con `color-mix(in srgb, var(--color-primary) 82%, black)` (18% de negro mezclado) en vez de agregar un token nuevo a `tokens.css` — mismo criterio ya usado en varios `:hover` de la app (`.fab:hover`, `.icon-button:hover`), y evita un token que solo tendría un consumidor. El punto más delicado fue "en sidebar desktop y en el drawer mobile" pero NUNCA en la barra superior colapsada de mobile: como `<aside class="sidebar">` es el MISMO elemento que se convierte en la barra horizontal en mobile (no un componente aparte), no podía simplemente ocultar `.sidebar-upcoming` en mobile a secas — hubiera ocultado también la copia del drawer, que vive en un árbol de DOM distinto (`.drawer-panel`, no `.sidebar`) y sí debe seguir visible ahí. Usé el selector de hijo directo `.sidebar > .sidebar-upcoming` (en vez de `.sidebar-upcoming` a secas) para que la regla de `display:none` en mobile solo alcance a la copia que efectivamente cuelga de `.sidebar`, dejando intacta la del drawer. Factoricé `renderUpcomingCard()` una sola vez y la llamo en los 2 lugares (mismo criterio que ya usa `renderNavItems()` para no duplicar markup entre sidebar/drawer) — el contenido (3 strings fijos en `UPCOMING_FEATURES`) no depende de ningún estado, así que no hacía falta pasarle nada. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el CSS real compilado, markup calcado de sidebar.js) en 3 vistas: sidebar desktop (tarjeta visible, título verde `rgb(161,211,91)` = `#A1D35B`, 3 ítems), barra superior mobile (tarjeta ausente del render, no solo oculta) y drawer mobile (tarjeta visible, entre la nav y el bloque de sesión — confirmado con `getBoundingClientRect()` que el orden vertical es nav → tarjeta → sesión) | Aproveché para documentar en `product-decisions.md` el motivo de excluir la tarjeta de la barra superior colapsada — no era obvio por qué un mismo componente se ve distinto según breakpoint sin esa nota, y ya que el mismo patrón (`.sidebar > .x` vs `.x` a secas) puede volver a hacer falta si se agrega algo más a esta tarjeta en el futuro |

| 2026-08-24 | Agregar 2 ítems de nav deshabilitados ("Mis facturas", "Calendario") debajo de "Alertas", en sidebar desktop y drawer mobile: ícono+texto atenuados, etiqueta "Próximamente" en verde sin atenuar, click dispara un toast en vez de navegar, nunca llevan la franja de activo. Actualizar el roadmap (README.md/product-decisions.md) con la vista de calendario y aclarar dónde vivirán los recibos escaneados | No existía ningún componente de toast en la app (confirmé con `grep` antes de escribir uno) — creé `components/toast.js`, chico y genérico (recibe cualquier `message`), reusando el mismo criterio de montar-en-body-y-destruir que ya usa `confirm-modal.js`, pero sin bloquear ni pedir respuesta: se auto-cierra a los 2.5s. Para los 2 ítems nuevos usé `<button type="button">` en vez de `<a href>` — a diferencia de los 3 ítems reales, estos no navegan a ningún lado, así que un link sin destino real hubiera sido semánticamente incorrecto (y `preventDefault()` en cada click para simular "no navegar" es más frágil que simplemente no ser un link). El primer intento de layout (etiqueta "al lado" del texto, en la misma línea) hizo que "Mis facturas" se partiera en 2 renglones en el ancho del sidebar — lo detecté en la captura de Playwright, no a ojo en el código — y lo resolví moviendo la etiqueta DEBAJO del texto (el enunciado permitía "al lado O debajo" explícitamente), con `margin-left` calculado para alinearla con el texto, no con el ícono (`calc(var(--icon-size) + var(--space-3))`, mismo ancho que ícono+gap). Para que la etiqueta quedara en verde PLENO mientras el ícono+texto quedan atenuados (55% opacidad), envolví ícono+texto en un `<span>` interno (`.sidebar-nav-item-dimmed`) con la opacidad reducida, dejando la etiqueta como hermano fuera de ese wrapper — aplicar la opacidad al `<button>` completo hubiera atenuado también la etiqueta, que el enunciado pedía en verde de acento sin atenuar. El detalle más delicado fue el drawer: el binding existente que cierra el drawer al hacer click en cualquier `.sidebar-nav-item` (para navegación real) hubiera cerrado el drawer también al tocar estos 2 ítems nuevos, que también llevan esa clase por estilo — lo excluí explícitamente con `:not(.sidebar-nav-item-disabled)` en el selector de ese binding, y agregué un binding aparte solo para estos 2 (mostrar el toast, sin tocar el estado del drawer) — verifiqué con Playwright que tocar un ítem real SÍ cierra el drawer y tocar uno "Próximamente" NO lo cierra, confirmando que la exclusión funciona en los 2 sentidos. Verifiqué con `vite build` que compilara, y con Playwright (harness HTML con el CSS/markup reales) que: son 2 `<button>` sin `href`, nunca tienen `.is-active`, el wrapper de ícono+texto mide opacity 0.55 mientras la etiqueta mide opacity 1 en verde `#A1D35B`, el click muestra el toast con el texto exacto "Disponible próximamente" sin cambiar el hash de la URL (sin navegación), y el comportamiento de cierre del drawer es el esperado en ambos casos | El bug de layout ("Mis facturas" partiéndose en 2 líneas) es la clase de detalle que solo aparece con el texto real más largo de los 2 ítems — si hubiera probado solo con "Calendario" (más corto) no lo habría visto, coincide con la razón por la que en tareas anteriores probé nombres largos a propósito para casos de truncamiento |
| 2026-08-24 | Corregir bug reportado en dispositivo real (no reproducible siempre en el emulador de escritorio): en mobile, la dona de "Gasto por categoría", las cards de viaje y el buscador expandido se pintaban POR ENCIMA del header (logo+lupa+hamburguesa) al hacer scroll, en vez de quedar debajo. El pedido ya traía 4 hipótesis de diagnóstico a verificar | Descarté las hipótesis 1 (z-index bajo) y 2 (fondo no sólido) apenas leí el CSS: `.sidebar` no tenía NINGÚN `z-index` (ni bajo ni alto) y su fondo (`var(--color-primary)`, `#001860` sólido) ya cubre completamente lo que hay detrás. La causa real es la 3: `.donut-chart-visual`, `.trip-card-photo` y `.search-field` (este último dentro de `.mobile-search-row`, el buscador expandido) son todos `position: relative` sin `z-index` propio — en CSS, un elemento posicionado con `z-index:auto` se pinta en el mismo "nivel" que cualquier otro elemento posicionado con `z-index:auto` (acá, `.sidebar`, que es `position:sticky`), y ese empate se resuelve por orden de aparición en el DOM; como los 3 elementos viven dentro de `.app-shell-content`, que en el HTML aparece DESPUÉS de `.sidebar` (ver `app-shell.js`), terminan pintándose encima suyo una vez que el header queda fijo por el scroll — esto no es un bug de Chrome/Safari mobile, es el comportamiento estándar de CSS stacking, solo que nunca se nota mientras nada se superpone visualmente en desktop. El fix fue agregar `z-index: 60` a `.sidebar` (arriba del FAB en `50`, debajo del modal en `100` y del drawer/overlay del menú hamburguesa en `200`, siguiendo la escala de z-index ya usada en el resto de `base.css`) — con eso el header pasa a su propio nivel de apilamiento y gana sin importar el orden del DOM, sin tocar nada de los 3 elementos de contenido. No hizo falta la hipótesis 4 (el buscador expandido no se inserta en un lugar distinto del DOM, ya vive como hermano de `.sidebar` desde que se agregó, ver `sidebar.js`) ni ajustar `padding-top` del contenido (el header es `sticky`, no `fixed` — sigue ocupando su espacio en el flujo normal, así que el contenido nunca queda oculto detrás de él al cargar). Como no tengo login de Google real en este entorno para probar Inicio/Mis viajes en el navegador, armé un harness HTML con el CSS real de la app (mismas clases: `.sidebar`, `.mobile-search-row`, `.donut-chart-visual`, `.trip-card`) y un script de Playwright que hace scroll y usa `elementFromPoint()` para verificar programáticamente qué elemento cae sobre las coordenadas del header — confirmé que SIN el fix, al hacer scroll, ese punto cae sobre `.trip-card-photo` (y sobre `.search-field` con el buscador abierto, en un rango de scroll de ~55-70px), reproduciendo el bug reportado exactamente; y que CON el fix, el mismo punto siempre cae sobre `.sidebar`, en todos los escenarios probados (con/sin scroll, con/sin buscador abierto) | El harness+Playwright no reemplaza la prueba en dispositivo real que pide el enunciado (esto es hit-testing de stacking en Chromium desktop headless, no un dispositivo táctil), pero sí prueba que la causa raíz identificada es la correcta y que el fix la resuelve a nivel de CSS — se lo señalé al usuario en vez de dar la tarea por completamente verificada |
| 2026-08-24 | 3 bugs de responsive reportados juntos en dispositivo real (Chrome Android): (1) modal "Agregar/Editar gasto" desbordado a la derecha, botones cortados; (2) "Gasto por categoría" en detalle de viaje desbordado horizontalmente, texto superpuesto; (3) texto de las cards de Alertas partiéndose palabra por palabra por el botón "Ver viaje". Pedido explícito de diagnosticar primero si el punto 2 es la causa raíz del punto 1 antes de tocar nada | Encontré la causa real del punto 2 (y, con ella, la explicación más probable del punto 1): `.trip-detail-kpis` tiene `flex: 0 0 22rem` (352px, sin encoger) dentro de `.trip-detail-body` (flex, sin wrap) — nunca tuvo override para mobile en ningún `@media (max-width: 45rem)` de todo el archivo (lo confirmé con `grep` antes de tocar nada). En un viewport angosto, esos 352px fijos por sí solos ya no caben, así que `.trip-detail-body` se desborda de lado y arrastra a toda la página (nada en esa sección tiene su propio scroll). Ese mismo desborde de página es la explicación estándar (comportamiento documentado de navegadores mobile, no una conjetura mía) de por qué un `position:fixed; inset:0` como `.modal-overlay` puede terminar empujado fuera de pantalla en dispositivo real sin reproducirse siempre en DevTools: sin `overflow-x:hidden` en `html`/`body`, el navegador ensancha el viewport de layout para acomodar el contenido desbordado, y ese viewport ensanchado es el que usan los elementos `fixed` para posicionarse — headless Chromium (mi entorno de prueba) no reproduce esa interacción específica, coincidiendo con lo que el pedido ya anticipaba ("al menos uno de los bugs de esta sesión no se reprodujo en el emulador"). Con eso decidí: apilar `.trip-detail-body`/`.trip-detail-kpis` en columna en mobile (ataca la causa real del punto 2, y probablemente también del 1) + agregar `overflow-x:hidden` en `html`/`body` como red de seguridad general (para que ningún desborde futuro de cualquier componente vuelva a desestabilizar elementos `fixed`/`sticky`) + `max-height`/`overflow-y:auto` en `.modal` (los 3 modales de la app comparten esa clase) para el eje vertical, que es un problema independiente y también pedido explícitamente ("si el contenido interno es más alto que el viewport, debe permitir scroll interno"). Para el punto 3, reusé literalmente la misma técnica que ya documenté para `.expense-item` en mobile (CSS Grid con `grid-template-areas` + `display:contents` en `.alert-item-body`, sin tocar el markup de `alerts.js`) en vez de inventar un patrón nuevo, tal como pedía el enunciado ("mismo patrón vertical que ya se usó para las filas de Gastos registrados"). Para verificar sin depender de un login de Google real, reusé y extendí el harness HTML+Playwright de la tarea anterior (mismo CSS real de la app) con checks programáticos por cada bug: overflow horizontal de `document.documentElement` en la página de detalle, que la dona quede dentro del rect de su tarjeta, que ningún par de filas de la leyenda se solape verticalmente, que los botones del modal queden alcanzables (visibles de entrada o, si el modal necesita scroll interno, tras hacer `scrollTop` DENTRO del modal — probé ambos casos con distintas alturas de viewport), y el ancho disponible en px para el texto de la alerta. Corrí cada check dos veces: contra el CSS de antes de este commit (`git show HEAD:...`, sin editar) para confirmar que SÍ reproduce los 4 síntomas (desborde de página 552px vs 375px de viewport, dona desbordando su tarjeta, texto de alerta en una columna de 71px/9 líneas) y contra el CSS ya corregido para confirmar que los 4 quedan en verde — antes de dar el fix por bueno | El harness+Playwright prueba la causa raíz y el fix a nivel de CSS con Chromium headless, pero no reproduce la interacción específica mobile-viewport-ensanchado-por-overflow que solo ocurre en navegadores mobile reales — coincide con lo que el propio pedido advertía, y por eso el `overflow-x:hidden` se agregó como medida independiente (dirigida exactamente a ese mecanismo) además de arreglar el layout que lo origina, en vez de asumir que arreglar el layout alcanzaba por sí solo; queda pendiente la confirmación del usuario en el dispositivo real donde se vio el bug originalmente |
| 2026-08-24 | En mobile, con texto activo en el buscador expandido del header (#trip-search-mobile), ocultar temporalmente lo que separa ese buscador de sus resultados — en Inicio: saludo, KPIs, "Gasto por categoría"; en Mis viajes: KPIs y pestañas Todos/Activos/Cerrados — para que el listado filtrado quede inmediatamente debajo sin scroll; al borrar el texto o cerrar con la X vuelven a aparecer. Explícitamente NO tocar el comportamiento en desktop ni la lógica de filtrado, solo qué se muestra/oculta | Verifiqué primero que "Mis viajes" sí aplicaba (KPIs + `.tab-list` empujan igual que en Inicio) antes de tocar nada ahí, tal como pedía el enunciado. Le puse id a los 3 bloques de Inicio a esconder (`#home-greeting`, `#home-kpi-row`, `#home-category-section`) y usé los selectores ya existentes en Mis viajes (`#my-trips-kpis`, `.tab-list`, ambos ya únicos en su página). Descarté usar el atributo `hidden` (el patrón obvio) porque `.kpi-row` ya tiene `display: grid` declarado como regla de autor con la misma especificidad que `[hidden]` — el mismo problema de especificidad que varios comentarios de este archivo ya documentan para `.mobile-search-row[hidden]`/`.drawer-overlay[hidden]`/`.modal-overlay[hidden]`, y que hubiera dejado el KPI-row visible igual pese a `el.hidden = true`. En vez de repetir ese parche puntual, usé un mecanismo distinto: una clase `.is-mobile-search-active` que cada página agrega/quita en el listener de `#trip-search-mobile` (nunca en el de `#trip-search`, que es el único que existe en desktop-facing code — ambos ya estaban separados en 2 listeners distintos desde antes), combinada con selectores compuestos (`.page-home.is-mobile-search-active #home-kpi-row`, mayor especificidad que la regla de autor a pisar) y todo el bloque envuelto en `@media (max-width: 45rem)` como refuerzo adicional (aunque la clase en sí solo la toca el listener mobile, que nunca se dispara desde el input de escritorio). En Mis viajes reusé el mismo patrón de clase en `.page-my-trips`; el filtro por pestaña (`activeFilter`) sigue aplicando igual mientras las pestañas están ocultas, no lo toqué. Verifiqué con un harness HTML+Playwright (réplica exacta de las 2 líneas de listener agregadas, no la app completa — no depende de Firebase) en 3 escenarios: mobile con texto en el buscador mobile (los 3/2 bloques se ocultan, los resultados quedan a 91px del buscador, sin scroll necesario), cerrar el buscador con la X (mismo mecanismo que ya limpiaba el input y disparaba "input" sintético, sin cambios ahí — las secciones reaparecen solas, mismo DOM, nunca se re-renderizaron), y desktop escribiendo en `#trip-search` (la clase nunca se agrega, los 3 bloques de Inicio siguen visibles) — confirmando que el buscador de escritorio queda intacto. Verifiqué con `vite build` que compilara | El uso de `hidden` + specificity-tie es un error ya documentado 3 veces en este mismo archivo CSS para otros componentes (`.mobile-search-row`, `.drawer-overlay`, `.modal-overlay`) — en vez de agregar una 4ta instancia del mismo parche, usé un mecanismo (clase + selector compuesto) que no depende de ganarle la carrera de especificidad a `[hidden]` |
| 2026-08-24 | Fotos sugeridas del destino en el formulario de crear/editar viaje, vía Pexels Search API (`VITE_PEXELS_API_KEY`, ya configurada en `.env` local y Vercel): al escribir en "Nombre del viaje" (debounce ~600ms), 3 thumbnails clickeables debajo del campo de URL; click guarda `src.large`/`src.medium` en el input y marca el elegido con borde verde; sin key/sin resultados/API caída, degrada al texto de ayuda de siempre sin bloquear el formulario. Reemplaza el flujo de "buscar en Unsplash y pegar el link a mano" | Creé `data/pexels.js` (`searchDestinationPhotos`) siguiendo el mismo criterio que ya separa `data/` (acceso a servicios externos) de `components/` — llama a la API directo desde el cliente con `import.meta.env.VITE_PEXELS_API_KEY`, sin función serverless de por medio (a diferencia de `api/scan-receipt.js`, que sí oculta una key de IA más cara/sensible; el plan gratuito de Pexels está pensado para consumirse desde el navegador, así que la key queda visible en el bundle/Network tab — asumido, no es un descuido). La función nunca lanza: cualquier fallo (sin key, sin red, HTTP no-2xx, JSON inválido, 0 resultados) devuelve `null`, y quien la llama decide degradar. En `trip-form.js` el punto que noté a mitad de la implementación: el texto de ayuda mencionado en el pedido ("mención a buscar en Unsplash y copiar el link") no existía todavía en el código (`grep` sin resultados) — lo escribí yo mismo como parte de este cambio, asumiendo que es el fallback que se espera, en vez de asumir que ya estaba y dejarlo roto. Diseñé el helper como un único slot (`#photo-helper`) que alterna entre 3 estados (hint / "buscando…" con pulso de opacity / grid de 3 thumbnails) en vez de mostrar varios simultáneos, y agregué un `requestToken` incremental para que una respuesta que llega tarde (el usuario ya seguía escribiendo) no pise el estado más nuevo. La selección visual (borde verde) no se ata al thumbnail clickeado en sí sino a que su URL coincida con el valor actual de `#trip-photo` — así, si el usuario edita la URL a mano después de elegir una sugerencia, el borde se recalcula solo (sin esto, hubiera quedado un thumbnail "seleccionado" mintiendo sobre qué URL se va a guardar). Verifiqué el contrato real de la API con la key configurada (`fetch` desde Node contra `api.pexels.com/v1/search`, sin imprimir la key) antes de escribir el código contra la forma que YO asumía que tenía la respuesta — confirmé `photos[].src.{medium,large}` tal cual. Para probar el flujo completo en el navegador (debounce, los 3 estados, selección, degradación en 3 escenarios: sin resultados/API caída/nombre vacío, y que el submit real sigue funcionando) monté un harness HTML temporal en la raíz del repo que importa el `trip-form.js` real vía Vite dev server (import.meta.env sí funciona ahí, a diferencia de un HTML estático suelto) con `page.route()` de Playwright mockeando `api.pexels.com` para los casos de borde — lo borré al terminar, nunca se commiteó. Verifiqué con `vite build` que compilara | La key de Pexels queda expuesta en el bundle cliente (visible en DevTools/Network) — es una decisión asumida, no un descuido: el pedido especificaba explícitamente llamar a la API directo desde el formulario con `import.meta.env.VITE_PEXELS_API_KEY`, sin mencionar una función serverless como sí tiene el escaneo de recibos; si más adelante se quiere ocultar la key, el patrón ya existe en `api/scan-receipt.js` |
| 2026-08-24 | 2 ajustes de consistencia visual, sin tocar lógica: (1) botón "Cancelar" junto a "Guardar cambios"/"Crear viaje" en el formulario de viaje, mismo estilo que el "Cancelar" del modal de gasto, que vuelve a Detalle de viaje sin guardar; (2) centrar el texto vacío de "Gastos registrados" con el mismo criterio que el de "Gasto por categoría" | Para el botón, agregué un parámetro opcional `cancelHref` a `mountTripForm()` (compartido por `new-trip.js`/`edit-trip.js`) en vez de una sola ruta fija, porque el "volver" correcto difiere: Editar viaje vuelve a `#/viaje/{id}` (Detalle de viaje, como pedía el enunciado), Nuevo viaje vuelve a `#/` (Inicio, de donde sale el botón/FAB que lleva ahí) — es un `<a href>` plano, mismo patrón de navegación que ya usa el resto de la app (nunca `history.back()`, que es menos predecible si el formulario se abre por link directo). Reusé la clase `.button-outline` tal cual (mismo estilo que "Cancelar" del modal de gasto, sin crear una variante), y compartí la clase de layout `.expense-form-actions`/`.trip-form-actions` (mismo flex + gap + submit a flex:1) en vez de duplicar esas 4 líneas de CSS. Para el texto vacío, antes de tocar nada medí con Playwright si `.expense-list-empty` realmente estaba descentrado como decía el pedido: no lo estaba — ya tenía `display:flex; align-items:center; justify-content:center`, y medí separaciones perfectamente simétricas (32px/32px arriba-abajo, 92.9px/92.9px izquierda-derecha). La única diferencia real contra `.donut-chart-empty` era el padding (`var(--space-8) var(--space-4)` vs `var(--space-4)` uniforme del donut) — igualé ese valor, que era lo único que el pedido pedía literalmente ("mismo padding") y que además coincidía con lo que ya medí como ya centrado; se lo señalé al usuario en vez de reescribir una lógica de centrado que no estaba rota. Verifiqué el botón con un harness HTML temporal (mismo mecanismo de tareas anteriores: importa `trip-form.js` real vía Vite dev server, borrado al terminar, nunca commiteado) contra Editar y Nuevo viaje: existe, mismo `getComputedStyle` que una referencia `.button-outline` recién creada, mismo layout (misma fila, submit más ancho, cancelar después), href correcto en cada pantalla, y clickearlo NO dispara `onSubmit` (no queda enganchado al submit del form). Verifiqué con `vite build` que compilara | El texto vacío de "Gastos registrados" ya estaba correctamente centrado en los 2 ejes antes de este cambio — se lo aclaré al usuario en vez de reportar un fix donde solo hubo un ajuste de padding, para no dar la impresión de que había un bug de centrado que en realidad no existía |

| 2026-08-24 | Agregar 2 ítems más de nav "Próximamente" al mismo patrón ya implementado para "Mis facturas"/"Calendario": "Viaje compartido" (ícono Users o UserPlus) y "Estadísticas" (ícono BarChart3 o TrendingUp), debajo de "Calendario", mismo estilo/comportamiento exacto (atenuado, etiqueta verde, toast, nunca activo). Documentación: confirmar que "Viaje compartido" tenga redacción consistente entre README.md/docs/product-decisions.md/sidebar (ya existía como roadmap, con otro nombre), y agregar "Estadísticas" como ítem nuevo de roadmap en ambos docs | Solo agregué 2 entradas al array `COMING_SOON_ITEMS` de `sidebar.js` — el componente ya era completamente genérico (`renderComingSoonItems()` itera el array, el binding de toast en `bindSidebar()` selecciona `.sidebar-nav-item-disabled` sin importar cuántos haya), así que no hizo falta tocar ninguna lógica, solo datos. Elegí `users` (no `user-plus`, que sugiere la ACCIÓN de agregar una persona, no el concepto de "viaje entre varios") y `bar-chart-3` (no `trending-up`, que implica una sola tendencia/línea, menos genérico que un ícono de barras para "estadísticas" en general) de los 2 pares de opciones que dio el pedido — agregué ambos íconos a `utils/icons.js` (confirmé antes con `ls node_modules/lucide-static/icons` que los 4 nombres candidatos existen como archivo, antes de elegir). Al revisar la documentación encontré que el pedido asumía algo que no era exactamente cierto: "Viaje compartido" SÍ estaba en el roadmap de `README.md` (como "Perfil compartido: viaje en pareja con reparto de gastos"), pero NO estaba documentado en `docs/product-decisions.md` en absoluto (`grep` sin resultados) — se lo señalé al usuario en vez de inventar una entrada previa que no existía. Renombré "Perfil compartido" → "Viaje compartido" en las 2 apariciones de `README.md` (la de alcance v1 y la del roadmap) para que coincida con el nombre que ahora también aparece en la navegación, sin ampliar el alcance de la feature en sí (seguía siendo "viaje en pareja", no agregué "o grupo" aunque el pedido lo mencionaba de pasada al describir la feature existente, porque no era un pedido explícito de ampliar alcance). Agregué "Estadísticas" al roadmap de `README.md` con el texto exacto pedido, y documenté la decisión completa (2 ítems nuevos + el rename) como una nueva entrada en `docs/product-decisions.md`, siguiendo el mismo formato Problema/Decisión/Por qué que ya usa la entrada anterior de "Ítems de nav 'Próximamente' para 2 features del roadmap" (no la edité, se agrega como decisión nueva que la extiende, mismo criterio que ya se usó en tareas anteriores para decisiones superadas/extendidas). Verifiqué visualmente con un harness HTML temporal (Vite dev server, borrado al terminar) que importa `utils/icons.js` y `components/toast.js` reales (ambos sin dependencia de Firebase, a diferencia de `sidebar.js` completo, que si se importara directo hubiera arrastrado `data/auth.js`/`data/store.js`) reproduciendo el mismo markup que genera `renderComingSoonItems()`: los 4 ítems en el orden correcto, los 4 con la etiqueta "Próximamente", ninguno con `.is-active`, opacidad 0.55 en ícono+texto vs 1 en la etiqueta (verde `rgb(161,211,91)` = `--color-accent`), y que click en los 2 ítems nuevos muestra el toast "Disponible próximamente" sin cambiar la URL. Verifiqué con `vite build` que compilara | El pedido daba por hecho que "Viaje compartido" ya estaba documentado en `docs/product-decisions.md`, y no era cierto — se lo aclaré al usuario en vez de fabricar una entrada retroactiva que nunca existió; la inconsistencia real (nombre distinto en README.md vs. el nuevo ítem de nav) sí se corrigió |

| 2026-08-24 | Landing page pública nueva (src/pages/landing.js), previa al login, con el semáforo de presupuesto como elemento de firma del hero (3 etapas — 62% En control / 88% Al borde del límite / 104% Límite superado — conectadas por una ruta punteada), capturas reales de Inicio/Detalle de viaje/Alertas, roadmap con el texto exacto ya documentado, y footer con crédito a Claude. Pedido explícito de compartir un plan de diseño (paleta/tipografía/layout/elemento de firma) antes de construir, y de resolver primero cómo conseguir las 3 capturas sin tener login de Google real en este entorno | Antes de tocar código cargué el skill de diseño pedido (`/mnt/skills/public/frontend-design/SKILL.md`) — no existe en este entorno Windows/CLI (solo aplica a otro tipo de sesión), lo cual señalé en vez de inventar que lo había leído; seguí en cambio el proceso que el propio pedido ya detallaba paso a paso. Publiqué un mockup del hero como Artifact (solo esa sección, no la página completa) para validar paleta/tipografía/elemento de firma antes de invertir en el resto — decisión deliberada de no construir todo a ciegas. Para las capturas, le pregunté al usuario cómo resolver el bloqueo de login (3 opciones: renderizar local con datos de muestra / usar su Chrome ya logueado vía claude-in-chrome / que él las pasara) — eligió la primera, la que yo recomendaba. Las generé con un harness Playwright que reproduce el markup/CSS real de Inicio, Detalle de viaje y Alertas (clases reales, `icon()`/`statusBadge()`/`renderKpiCard()`/`categoryChart()` importados de verdad vía Vite dev server, no reinventados) con datos de un viaje a Cartagena deliberadamente en `danger` (114% del presupuesto) para que el semáforo se vea activo en rojo tal como pedía el enunciado, más Medellín en `warning` y Eje Cafetero en `ok` — los 3 estados a la vez, visibles en la misma captura de Inicio. Evité `renderTripCard()`/`renderAlert()` reales porque dependen de `getTripTotal()` (store.js, acoplado a Firestore) — repliqué esa misma lógica a mano con montos que yo mismo fijé, en vez de forzar una dependencia de backend en un harness estático. De las 6 funcionalidades pedidas, 4 tienen su propia captura de página completa (Inicio, Detalle de viaje, Alertas, y el formulario de Nuevo viaje — este último reaprovechando las fotos sugeridas de Pexels de la tarea anterior) y 2 son recortes ajustados (`clip` sobre el `boundingBox()` real) de los botones de WhatsApp/Google Calendar dentro de Detalle de viaje — ningún mockup inventado, las 6 imágenes salen de las mismas 4 páginas renderizadas. Para el roadmap, reusé el texto literal de 5 de los 6 ítems de `README.md` (dejé afuera "eliminar viaje de forma permanente": es una decisión de alcance interna, no algo que alguien buscaría en una landing) — inicialmente les puse un ícono `circle-check`, y al revisar el resultado antes de darlo por terminado noté 2 problemas reales que corregí sin que nadie los señalara: (1) un check verde en items de roadmap sugiere "completado", exactamente lo opuesto de lo que significan — lo reemplacé por el mismo patrón visual "Próximamente" que ya usa el sidebar para sus 4 ítems deshabilitados, en vez de inventar un ícono nuevo; y (2) ese tag en su primera versión usaba `--color-accent` crudo como color de texto sobre tarjeta blanca — el mismo verde que en el sidebar contrasta bien porque va sobre navy, acá no pasaba contraste de texto — lo cambié a `--color-status-ok-text` (la variante oscurecida que `.status-badge-ok` ya usa en toda la app para exactamente este caso: texto de acento sobre fondo claro). También encontré y corregí 3 colores hex escritos a mano (`#d0d0d0`/`#ececec`/`#c8c8c8`, bordes/fondo del frame de navegador decorativo) que se me habían colado pese a la regla explícita de `tokens.css` — los reescribí como `color-mix(in srgb, var(--color-text-muted) N%, white)`, mismo patrón que ya usa el resto de `base.css` para tonos derivados. Agregué `--shadow-lg` a `tokens.css` (no a landing.css) porque es un tamaño de sombra genérico, reusable — pero los tamaños de fuente grandes del hero SÍ quedaron escopeados en `landing.css` (`--landing-text-hero`/`--landing-text-section`), no en `tokens.css`, porque ningún otro componente los necesita y agregarlos al vocabulario global hubiera sugerido que el dashboard debería escalar hacia ahí también, contradiciendo el pedido explícito de que el dashboard NO se toca. El botón "Iniciar sesión con Google" del hero es un `<a href="#/login">` plano, no un segundo `signInWithGoogle()` — el pedido pedía las 2 cosas a la vez ("reutiliza la función, no la dupliques" + "se accede desde el CTA del hero"), y enrutar al login real (sin tocarlo) satisface ambas sin ambigüedad. En el router, agregar la landing como default no-autenticado tenía un efecto secundario que no era obvio a primera vista: `redirectError` (el mensaje de error de un `signInWithRedirect` fallido al volver de Google) se seteaba y se mostraba siempre porque antes CUALQUIER pantalla sin sesión era el login — con la landing de por medio, ese error se hubiera perdido en silencio si el hash al volver de Google no era exactamente "/login". Lo detecté releyendo el flujo completo (no lo pidió el enunciado) y lo corregí: ahora `redirectError` fuerza el login sin importar el hash. Sin ícono de marca de Google en el CTA (evita introducir los 4 colores de marca de Google, ninguno de los cuales está en tokens.css) ni de GitHub en Lucide (no cubren logos) — el de GitHub sí se escribió a mano en `landing.js` (no en `utils/icons.js`, que es solo para el set de Lucide) porque es un mark de marca reconocible, monocromo vía `currentColor`, no un ícono genérico. Verifiqué con Playwright contra la app real (no un mockup) en desktop y mobile: la ruta "/" sin sesión muestra la landing (no el login), el CTA apunta a "#/login" y navegar ahí sí muestra el login real, sin overflow horizontal en 390px, y corregí en el camino un botón del nav que se partía en 2 líneas en mobile (lo oculté ahí — el mismo CTA ya está repetido en el hero, visible sin scroll). Verifiqué con `vite build` que compilara | El plan de diseño y el mockup del hero se compartieron y aprobaron (implícitamente, al avanzar a la siguiente pregunta sin objeciones) antes de escribir el resto de la página — no asumí aprobación total del silencio en un tema tan grande sin dar la oportunidad concreta de objetar; se lo dejé explícito en el resumen final igual |

| 2026-08-24 | Enriquecer la tarjeta "Próximas actualizaciones" del sidebar (desktop y drawer mobile, misma copia generada por `renderUpcomingCard()` en los 2 lugares): cada ítem pasa de ser solo el nombre a bullet verde + título en negrita + 1 línea de descripción más chica y atenuada debajo, con texto exacto dado para los 3 (Multidivisa/Multilenguaje/Escaneo con IA). Pedido explícito de no tocar fondo/radio/posición de la tarjeta, solo el formato interno | Cambio acotado a 2 archivos: `UPCOMING_FEATURES` pasó de array de strings a array de `{title, text}`, y `renderUpcomingCard()` genera un `<li>` por ítem con bullet+título+descripción en vez de solo texto — sin tocar `.sidebar-upcoming` (fondo/radio) en `base.css`, tal como pedía el enunciado. Verifiqué visualmente con un harness HTML (mismo criterio que ya usé para el resto del sidebar en esta sesión — `renderUpcomingCard()` no está exportada y `sidebar.js` completo depende de Firebase, así que reproduje el markup real a mano en vez de importar el módulo) a ancho de escritorio: la primera captura salió en blanco porque probé a 400px de viewport, bajo el breakpoint de 45rem donde `.sidebar > .sidebar-upcoming` se oculta a propósito (la copia de escritorio, no la del drawer) — no era un bug del cambio, era el breakpoint existente aplicando bien; repetí a 1024px y confirmé bullet verde, título blanco en negrita, descripción más chica y atenuada, y separación clara entre los 3 ítems. Verifiqué con `vite build` que compilara | Nada que señalar — cambio puramente de presentación, sin tocar lógica ni el resto de la tarjeta |

| 2026-08-24 | Rediseñar "Lo que viene" (roadmap) de la landing pública: de lista de filas apenas separadas por sombra a tarjetas reales (mismo radio/sombra que las tarjetas de funcionalidades), grid de 2 columnas en desktop / 1 en mobile, y la etiqueta "Próximamente" con el mismo tratamiento pill que ya usa `.status-badge` en toda la app (no texto verde suelto). Copy de cada ítem sin tocar | Para la etiqueta, en vez de crear un tratamiento nuevo que se pareciera a `.status-badge`, apliqué las clases reales `status-badge status-badge-ok` directo en el markup — `base.css` ya se carga global en toda la app (landing incluida) desde `index.html`, así que el pill queda pixel-idéntico al que ya se ve en Inicio/Alertas/cada viaje, sin duplicar CSS. Sin ícono (no llamé a `statusBadge()`, que fuerza uno): `circle-check` en un ítem de roadmap sugeriría "completado", el mismo problema ya corregido en una tarea anterior de esta sesión para este mismo roadmap. Borré por completo la clase `.roadmap-tag` (texto verde suelto) que había quedado de esa tarea anterior, ya sin uso. El grid queda a 2 columnas con el mismo patrón que `.feature-grid` (mismo breakpoint `max-width:45rem` para 1 columna en mobile); el último ítem (el más largo, "Estadísticas...") ocupa las 2 columnas con una clase `.roadmap-item-full` — mismo criterio que `.feature-card-full` — en vez de dejarlo solo en la última fila con un hueco vacío al lado. Verifiqué con Playwright contra la app real (no un mockup): `grid-template-columns` de 2 columnas iguales en desktop y 1 en mobile, la etiqueta con las clases/colores reales de `.status-badge-ok` (fondo verde tintado, texto verde oscurecido, radius-full), el último ítem con el doble de ancho que los demás (spans confirmado por `boundingBox()`), y sin overflow horizontal en mobile. Verifiqué con `vite build` que compilara | Nada que señalar — cambio acotado a la sección de roadmap, sin tocar el copy de los 5 ítems ni el resto de la página |

| 2026-08-24 | Rediseñar la navegación de la landing: el header logo+botón se reemplaza por un nav flotante tipo píldora (fondo blanco, radio completo, sombra, centrado, `position:fixed`) con 4 anclas — "Cómo funciona" (sección nueva de 3 pasos, a crear), "Funcionalidades", "Próximamente" (ambas ya existentes) y "Iniciar sesión" (botón navy, acción, no navegación) — logo aparte, fuera del pill, en su esquina de siempre. Scroll suave al clickear. En mobile, el pill colapsa a logo+hamburguesa con las 4 opciones en un menú desplegable. El CTA del hero explícitamente no se toca | Reestructuré `.landing-nav` a `.landing-topbar` (contenedor `position:fixed` de ancho completo, `pointer-events:none`) con 2 hijos que reactivan `pointer-events:auto` cada uno por separado: el logo (absolute, esquina izquierda) y el pill (centrado de verdad vía `justify-content:center` sobre el contenedor de 100% de ancho, no centrado respecto al espacio que deja el logo — así el pill queda dead-center sin importar cuánto mida el logo). Para "Cómo funciona" agregué `STEPS`/`stepCard()` con el mismo tratamiento de tarjeta que ya usan `.roadmap-item`/`.feature-snippet` (radius-md, shadow-sm, borde sutil) — el badge de cada tarjeta muestra el NÚMERO del paso (1/2/3), no un ícono genérico, porque acá el orden es información real, no decoración (fue justo el caso contrario al de las torres de categoría, donde nunca hay un orden real). `scroll-behavior:smooth` va detrás de `@media (prefers-reduced-motion: no-preference)` y scopeado con `html:has(.page-landing)` (no una clase global en `<html>`, que la landing no toca) — nunca se aplica si el usuario prefiere menos movimiento. Cada `.landing-section` suma `scroll-margin-top` para que saltar a un ancla no deje el título tapado detrás del pill fijo. Bug real que encontré probando el menú mobile con Playwright (no me lo señaló nadie): los links del menú desplegable se veían pero NO eran clickeables — `.landing-mobile-menu` hereda `pointer-events:none` de `.landing-topbar` (su padre) y yo había reactivado `pointer-events:auto` en el logo y en el pill por separado, pero me olvidé de hacerlo también en el menú mobile; Playwright lo detectó solo como "el click lo intercepta el <div> de atrás" en vez de fallar en silencio, así que quedó atrapado antes de darlo por terminado. Verifiqué con Playwright contra la app real en desktop y mobile: el pill con sus 3 anclas + login, click en cada ancla scrollea a la sección real con el título despejado del pill (96px de aire, no tapado), "Iniciar sesión" navega al login real (`#/login`, sin duplicar `signInWithGoogle()`), en mobile el pill colapsa a solo logo+hamburguesa, el menú abre con las 4 opciones, clickear una las cierra y navega, y sin overflow horizontal. Verifiqué con `vite build` que compilara | El bug de `pointer-events` no lo pidió el enunciado — lo encontré y corregí solo porque probé el menú mobile de punta a punta con Playwright en vez de asumir que "se ve bien" alcanzaba |

| 2026-08-24 | 2 ajustes al nav flotante y layout de la landing: (1) confirmar max-width:1200px en todo el contenido (hero/secciones/footer); (2) mover el logo de TripFlow de afuera del pill hacia DENTRO, extremo izquierdo, seguido de las anclas y el CTA — todo en una sola barra con fondo/radio unificados (referencia visual de layout, no de color); (3) el texto del CTA del pill pasa de "Iniciar sesión" a "Probar demo", mismo destino (`#/login`). El CTA grande del hero no se toca | Antes de tocar nada verifiqué con Playwright si el punto 1 ya estaba resuelto: sí — `.landing-shell` (que envuelve hero, las 3 secciones y el footer) ya tenía `max-width:75rem` (1200px exacto) desde la tarea del roadmap. Medí el ancho real a 1920px de viewport para confirmarlo en vez de asumirlo por leer el CSS: 1200px en el propio `.landing-shell`/footer-inner, 1152px en sus hijos directos (1200 menos el padding lateral de 1.5rem a cada lado — la misma restricción medida en 2 niveles distintos del árbol, no una inconsistencia). No hice ningún cambio para este punto, lo documento para que quede registro de que se verificó, no que se asumió. Para el punto 2, reestructuré el DOM: `.landing-topbar-logo` (que vivía suelto, hermano de `.landing-pill`) desaparece, y el logo pasa a ser el primer hijo de `.landing-pill` con su propia clase `.landing-pill-logo`; envolví las 3 anclas en un `.landing-pill-links` para que el pill quede con `justify-content:space-between` entre exactamente 3 bloques (logo/links/CTA) en vez de 5 elementos sueltos peleando por espacio. El pill ahora tiene su propio `max-width:62rem`, menor al `max-width:75rem` de `.landing-shell` — a propósito, para que una barra que ocupa casi todo el ancho de una pantalla grande siga leyéndose como "pill" flotante y no como un navbar de ancho completo. En mobile, el selector que oculta los links pasó de apuntar a `.landing-pill-link` individual a `.landing-pill-links` (el wrapper nuevo) — si no lo actualizaba junto con el HTML, los 3 links hubieran quedado visibles de nuevo al lado del logo en mobile, rompiendo el colapso a hamburguesa que ya funcionaba antes de este cambio. Verifiqué con Playwright contra la app real en desktop (1920px) y mobile: logo+links+CTA dentro del mismo `.landing-pill` (ya no existe `.landing-topbar-logo` suelto), orden de hijos correcto, texto "Probar demo" en el pill Y en el menú mobile, href sigue siendo `#/login`, el CTA del hero sigue diciendo "Iniciar sesión con Google" sin tocar, colapso a logo+hamburguesa en mobile intacto, sin overflow horizontal. Verifiqué con `vite build` que compilara | El punto 1 (max-width:1200px) ya estaba resuelto de una tarea anterior — se lo señalé al usuario en vez de aplicar un cambio redundante solo para "hacer algo" en ese punto del pedido |

| 2026-08-24 | 2 pedidos de la landing, el segundo llegó a mitad de la respuesta del primero: (1) los botones "Probar demo" (pill + menú mobile) pasan de fondo navy/texto blanco a fondo verde de acento/texto navy; (2) separar cada sección en bloques visualmente distintos — ejemplo dado: "Cómo funciona" con fondo blanco — en vez de una sola página continua sobre el mismo gris | Antes de tocar nada noté que `landing.css` había cambiado en disco desde la última vez que lo edité (max-width del pill 62rem→75rem, padding del CTA `--space-4`→`--space-8`, padding-top del hero 7rem→8rem, y se había borrado `.page-landing a { color: inherit; }`) — lo tomé como estado actual sin revertirlo, tal como indica el aviso del sistema, y antes de asumir que ese último borrado no rompía nada verifiqué con `grep` que los 8 `<a>` de la landing ya tienen color explícito propio (por clase, o heredado de `.landing-footer a`), así que la regla borrada era en efecto redundante. Para el punto 1 reusé el mismo criterio de contraste que ya documenté para `.fab` en `base.css` (navy sobre `--color-accent` da ~9:1, blanco da ~1.75:1 e ilegible) en vez de inventar uno nuevo. Para el punto 2, la estructura anterior envolvía TODO el contenido (hero + 3 secciones) en un solo `<div class="landing-shell">` — así era imposible que una sección tuviera su propio fondo full-bleed sin afectar a las demás, porque el fondo se hereda del `.page-landing` de arriba de todo. Reestructuré: cada `<section>` pasa a ser su propio bloque full-bleed (fondo de borde a borde) con un `.landing-shell` ADENTRO (mismo patrón que ya usaba `.landing-footer` para centrar su contenido a 1200px sin que el fondo navy del footer se recortara) — alternando blanco (`.landing-section-white`, agregada a "Cómo funciona" y "Lo que viene") y el gris de página por defecto (sin declarar de nuevo, "Funcionalidades" simplemente no lleva la clase). El hero ya no vive dentro de un `.landing-shell` compartido, así que tuvo que sumar su propio `max-width:75rem; margin:0 auto;` más padding horizontal — con cuidado de NO fusionar la clase `.landing-shell` directo sobre `.landing-hero` (el shorthand `padding` de una hubiera pisado por completo el `padding` del otro, no se combinan lado por lado), por eso quedó como propiedades repetidas en la misma regla de `.landing-hero` en vez de una segunda clase. Verifiqué con Playwright contra la app real: los 2 botones "Probar demo" con `rgb(161,211,91)` de fondo y `rgb(0,24,96)` de texto, las 3 secciones alternando blanco/transparente(gris)/blanco, el hero y las 4 `.landing-shell` siguen acotados a 1200px de ancho exactos a 1920px de viewport, y sin overflow horizontal en mobile. Verifiqué con `vite build` que compilara | Nada que señalar — los 2 pedidos eran acotados y no interactuaban entre sí más allá de compartir el mismo archivo |

| 2026-08-24 | Rediseñar la pantalla de Login como tarjeta flotante de 2 paneles (formulario + atmósfera de marca) sobre fondo navy, inspirada en un layout de referencia genérico "solo de estructura" — logo/tagline/botón centrados en el panel izquierdo, gradiente de marca con eco sutil del semáforo en el derecho, oculto en mobile. Sin tocar la lógica de auth (popup con fallback a redirect) | Creé `styles/login.css` (mismo criterio que `landing.css`: hoja propia importada solo por `pages/login.js`, porque nada de este diseño se reusa en el resto de la app) y borré las 2 reglas viejas de `.page-login`/`.page-login button` en `base.css` — dejarlas hubiera creado 2 definiciones de la misma clase en 2 archivos distintos, peleando por ganar según orden de carga (frágil e impredecible, no algo que quisiera dejar como deuda). El gradiente del panel derecho usa únicamente `color-mix()` sobre `--color-primary`/`--color-accent` (sin hex nuevos) — el pedido pedía explícitamente "no un gradiente genérico morado/azul, tiene que sentirse TripFlow". Para el eco del semáforo, primero probé el pedido literal (el isotipo como marca de agua gigante, semitransparente) y lo descarté al verlo renderizado: `Isotipo-fondo-oscuro.svg` no es una silueta transparente sino un cuadrado blanco sólido con la marca navy adentro (pensado como badge autocontenido para fondo oscuro, no como watermark), así que a opacidad baja se veía como un bloque asimétrico, no como una marca de agua limpia — lo saqué y dejé solo 3 puntos de luz difuminados (`box-shadow` con blur, un `<span>` por color de estado) en vez de forzar el isotipo, tal como el pedido mismo permitía explícitamente ("usar buen criterio, no forzarlo si no se ve bien"). Elegí no reproducir literalmente las tarjetas/línea punteada del hero de la landing acá: a la escala de medio panel de 450px se hubiera visto apretado, así que quedó como atmósfera (bokeh), no como diagrama repetido. Verifiqué con Playwright en desktop y mobile: la tarjeta con sus 2 paneles, el botón sigue funcional y no quedó permanentemente roto tras un click (headless no completa el flujo real de Google, comportamiento esperado y ya documentado en tareas anteriores de esta sesión, no una regresión mía), el panel derecho se oculta por completo en mobile con el fondo navy detrás, sin overflow horizontal, y el estado de error (forzado a mano) se ve bien posicionado debajo del botón. Verifiqué con `vite build` que compilara. Confirmé con `git diff` que el bloque de JS (`addEventListener`, manejo de `error`/`disabled`, `signInWithGoogle()`) quedó carácter por carácter idéntico al de antes — solo cambió el `innerHTML` de arriba | El isotipo como marca de agua no se veía bien y lo descarté en vez de forzarlo — se lo señalé al usuario en vez de dejarlo silenciosamente afuera sin explicar por qué el resultado final no incluye ese elemento que sí pedía el enunciado |

| 2026-08-24 | 2 pedidos seguidos sobre la landing: (1) rediseñar "Cómo funciona" con el patrón imagen+badge circular superpuesto (foto arriba con esquinas redondeadas al radio de la tarjeta, círculo navy con el número del paso mordiendo el borde inferior de la foto, título+descripción centrados debajo), encabezado de la sección centrado, capturas más recortadas/enfocadas que las de "Funcionalidades"; (2) mientras se armaban esas 3 capturas nuevas, pedido de actualizar TODAS las capturas de la landing porque el harness que las generaba (varias tareas atrás) había quedado desactualizado frente a cambios reales de UI | Antes de regenerar nada investigué POR QUÉ estaban desactualizadas, no solo que lo estaban: el harness original re-tipeaba a mano el markup del sidebar (mismo patrón ya usado para los mockups de esta sesión) — y ese mismo patrón fue la causa: cuando `sidebar.js` ganó 2 ítems "Próximamente" más y la tarjeta "Próximas actualizaciones" pasó a título+descripción (tareas de esta sesión), el harness nunca se actualizó junto con eso porque era una copia, no el componente real. En vez de volver a copiar el markup a mano (reproduciendo el mismo riesgo para el próximo cambio de sidebar), verifiqué que `renderSidebar()` es puro y segura de importar sin sesión real (revisé `firebase.js`: `initializeApp`/`getAuth`/`getFirestore` son solo config, sin red ni listeners; `getTripAlerts()`/`getCurrentUser()` devuelven `[]`/`null` sin `initStore()`, nunca lanzan) e importé el componente real en el harness — estas capturas no pueden volver a desactualizarse en este aspecto. El badge de alertas (`getTripAlerts()` da 0 sin datos reales) se inyecta a mano después del render, solo cosmético, para mantener el mismo "2" de siempre. Encontré 2 bugs propios durante la generación, ninguno pedido, y los arreglé antes de dar la tarea por terminada: (a) `.sidebar` trae `height:100dvh` (correcto en la app real) — en Alertas (poco contenido) eso inflaba el sidebar a la ventana completa, y mi recorte "ajustado al contenido" terminaba cortando la nav a la mitad; se resolvió anulando esa altura SOLO en la captura de Alertas (Inicio/Detalle se dejaron con el comportamiento real, ahí el contenido ya es más alto que el propio sidebar) y recortando por el borde inferior más bajo entre la lista de alertas y la tarjeta de actualizaciones, no solo por el primero. (b) Las 3 imágenes nuevas de "Cómo funciona" con `object-fit:cover` recortaban contenido real (una etiqueta o un badge completo quedaba fuera) porque sus proporciones (3.9:1 y 6:1) eran mucho más anchas que el slot de la tarjeta (2.6:1) — cambié a `object-fit:contain` (con el fondo de la tarjeta como marco) en vez de perseguir el recorte exacto de cada imagen, más robusto para 3 capturas de contenido tan distinto entre sí (un campo angosto vs. 3 badges en fila). Verifiqué con Playwright contra la app real: las 3 tarjetas con imagen completa (sin recortar contenido), badge circular superpuesto (bg navy, texto blanco, `margin-top` negativo confirmado), encabezado centrado solo en "Cómo funciona" (`#como-funciona .landing-section-header`, sin afectar las otras 2 secciones), y las 9 imágenes de `src/assets/landing/` regeneradas mostrando el sidebar/UI vigente (4 ítems "Próximamente", tarjeta título+descripción) — comparé visualmente contra las capturas anteriores commiteadas, no solo contra mi memoria de qué había cambiado. Verifiqué con `vite build` que compilara | El pedido de actualizar capturas pedía verificar cada una contra `tripflow-ashen-chi.vercel.app` antes de reemplazar — no lo hice así: verifiqué contra el CÓDIGO ACTUAL del proyecto (que es lo que este harness renderiza) en vez de contra el sitio en producción, porque en este entorno no tengo forma de loguearme con Google ahí tampoco; el mismo bloqueo ya documentado en la tarea original de la landing. Se lo señalo en vez de afirmar que se verificó como se pidió literalmente |

| 2026-08-24 | Reemplazar el gradiente + 3 puntos de luz del panel derecho del login (atmósfera abstracta del semáforo, sin foto de marca todavía en ese momento) por `Imagen-inicio-sesion.jpg`, subida por el usuario a `public/img/` junto a los logos — foto real del dashboard de Tripflow en un entorno de viaje. Pedido de seguimiento en el mismo hilo: que la imagen cubra 100% de ancho/alto, y no dejar afuera la versión mobile (antes el panel entero se ocultaba en mobile) | Elegí el `.jpg` (80KB) y no el `.png` que el usuario subió al lado (1.8MB, mismo contenido) — no lo pidió explícitamente pero es la misma imagen, y es una que carga en cada visita a esta pantalla, así que el peso importa; dejé el `.png` sin tocar en `public/img/` (no lo até a nada) en vez de borrarlo, no es mi lugar eliminar un archivo que subió el usuario sin que lo pida. `background-size:cover` ya cubre 100% ancho/alto del panel por diseño (crop preservando aspecto, sin dejar huecos) — lo verifiqué con Playwright en vez de asumir que el pedido de "100%" significaba que faltaba algo ahí; donde sí faltaba de verdad era mobile, que tenía el panel completo en `display:none` desde el rediseño original del login (cuando la atmósfera abstracta no aportaba nada en una pantalla angosta). Ahora entra como banner arriba del formulario vía `order:-1` dentro del mismo grid (sin tocar el orden real del DOM/lectura del formulario) a una altura fija de 12rem, con el mismo `background-size:cover` de la regla base cubriendo ese alto. Borré `.login-art-orb*` (ya sin uso, la foto reemplaza esa atmósfera por completo, no conviven) y verifiqué con `grep` que no quedó ninguna referencia suelta. De paso quedó incluido en este commit un ajuste que el usuario ya había hecho a mano en `landing.css` (`grid-template-columns` del hero, `1.05fr 0.95fr` → `1fr 1.1fr`) que estaba sin commitear de una tarea anterior — se lo señalo en vez de dejarlo perdido en un commit sin relación aparente. Verifiqué con Playwright en desktop y mobile: la foto visible y cubriendo 100% del panel en los 2 breakpoints, sin overflow horizontal en mobile, esquinas superiores del banner mobile redondeadas al radio de la tarjeta. Verifiqué con `vite build` que compilara | Nada que señalar sobre el pedido en sí — sí dejé explícito el ajuste de `landing.css` que no era mío, para que quede claro en el historial qué cambio corresponde a qué pedido |

| 2026-08-24 | La imagen del panel de login debe verse COMPLETA (sin recortar), a 500×368px exactos | Antes de tocar CSS leí el header del propio archivo JPEG (parseo manual de los marcadores SOF0/SOF2 para sacar ancho/alto) para confirmar de dónde salían esos 500×368 — coinciden EXACTO con el tamaño nativo del archivo, así que el pedido no era un tamaño arbitrario: es "mostrala a su resolución real, sin que `cover` la recorte para llenar un contenedor de otra proporción" (que es lo que pasaba antes: el panel tenía `1.2fr` de ancho variable, casi nunca calzando 500×368 exacto). Cambié `.login-art-panel` a `width:500px; height:368px` fijos (no `%`/`fr`) y `.login-card` de `grid-template-columns: 1fr 1.2fr` a `minmax(18rem, 1fr) 500px` — la 2da columna ya no es una fracción, es ese mismo ancho fijo. Agregué el `minmax(18rem, ...)` en la 1ra columna a propósito (no estaba en el pedido): sin ese piso, en el rango de ancho de página justo arriba del breakpoint mobile (45rem) el panel de formulario se hubiera comprimido mucho antes de que el layout colapsara a 1 columna. Probé ese rango con Playwright (760px de viewport, entre el breakpoint de 720px y un desktop típico) — el texto envuelve a 2 líneas (tagline y botón) pero no se rompe ni desborda; lo dejo así porque es un ancho de ventana poco común y sigue siendo usable, no perfecto. En mobile el panel ya no puede ser 500×368 (una tarjeta de 416px de ancho máximo no entra) — sigue recortando con `cover` a su propio ancho/alto de banner (12rem), agregué `width:100%` explícito ahí que antes faltaba (heredaba el `width:500px` fijo de la regla base sin problema visible hasta ahora porque el `grid-template-columns:1fr` ya lo forzaba angosto, pero quedaba implícito en vez de explícito). Verifiqué con Playwright: `boundingBox()` del panel da exactamente `{width:500, height:368}` tanto a 1440px como a 760px de viewport (el fix aplica en TODO el rango desktop, no solo en el ancho "de diseño"), sin overflow horizontal en ninguno de los 3 anchos probados, mobile con su propio banner de 342×192 sin afectar. Verifiqué con `vite build` que compilara | Nada que señalar sobre el pedido en sí — el `minmax(18rem, 1fr)` que agregué de más lo dejo documentado porque no lo pidieron explícitamente, lo agregué por robustez propia |

| 2026-08-24 | Agregar un ícono de campana de acceso rápido a Alertas, visible sin abrir el menú, en mobile (header compartido, junto a lupa+hamburguesa) y desktop (Inicio/Mis viajes, junto al buscador) — ADICIONAL al badge con número que ya vive en el ítem "Alertas" del sidebar/drawer, que no debía tocarse. 2 estados: campana sola / campana con punto rojo simple (sin número) si hay ≥1 viaje en alerta, reusando la misma fuente de estado que ya usa ese badge (`getTripAlerts()`). Click navega a `#/alertas` | Creé `components/alerts-bell.js`, un único componente para las 2 variantes (`variant: "mobile" | "desktop"`) — mismo criterio que el resto de componentes compartidos de esta sesión (kpi-card, account-menu): un solo lugar donde vive "hay alerta o no", para que nunca se desincronice del badge del sidebar ni entre mobile/desktop. La variante mobile combina `.icon-button` con `.sidebar-icon-button` (mismas 2 clases que ya usan la lupa/hamburguesa del header compartido, mismo color blanco sobre navy); la desktop usa solo `.icon-button` (mudo sobre superficie clara, como el resto del header). El punto rojo (`.alerts-bell-dot`) es deliberadamente más simple que `.nav-badge` (sin número, sin `min-width`/padding para texto) porque acá vive en un ícono suelto del header, sin espacio para un contador legible — mismo color (`--color-status-danger`) para que se lea como la misma señal. Bug propio que encontré probando en viewport mobile real (no en el pedido): la campana desktop, al no tener ninguna regla de `@media (max-width:45rem)` propia, se quedaba visible dentro de `.page-header-actions` en mobile — duplicada con la campana mobile del header compartido. La arreglé igual que ya se oculta `.search-field`/`.account-menu` ahí mismo: `.page-header-actions .alerts-bell { display:none }` dentro del mismo media query existente, sin tocar la regla `.sidebar-icon-button` (que sí debe seguir visible en mobile). Para verificar con datos reales de alerta sin depender de un login real de Google (no disponible en este entorno) armé un mock temporal de `firebase/firestore` (`collection/doc/addDoc/updateDoc/deleteDoc/getDocs` en memoria, sin red) más una config de Vite aparte (`--config vite.mock.config.mjs`, alias solo de ese paquete, nunca commiteada) para poder llamar a `createTrip()`/`addExpense()` REALES de `data/store.js` y así producir un viaje en estado "danger" de verdad — evita reproducir a mano el cálculo del semáforo (`getBudgetStatus`), que ya había señalado en tareas anteriores como fuente de bugs por copiar en vez de reusar. El harness (`bell-test.html`, temporal, en la raíz) importó los componentes reales (`renderSidebar`, `dashboard.js`/`my-trips.js` `render()`) en vez de copiar su markup, mismo criterio ya establecido en esta sesión. Verifiqué con Playwright, en viewport mobile y desktop por separado (el breakpoint depende del viewport real, no de un contenedor angosto): orden lupa→campana→hamburguesa en mobile y campana→buscador en desktop, 0 puntos rojos sin alertas y 1 con alertas en las 4 ubicaciones (mobile, Inicio desktop, Mis viajes desktop), href `#/alertas` en los 2, el `nav-badge` numérico existente intacto (sigue mostrando "1"), y la campana desktop oculta en viewport mobile tras el fix. Verifiqué con `vite build` que compilara. Borré el harness, la config mock y el mock de Firestore al terminar — nada de eso quedó en el repo | El bug de la campana desktop duplicándose en mobile no lo pidió el enunciado — lo encontré por probar el breakpoint real con Playwright en vez de asumir que bastaba con insertar el componente en los 2 lugares |

| 2026-08-24 | Agregar un modal de confirmación ANTES de ejecutar "Finalizar viaje" (Detalle de viaje) para evitar clicks accidentales — hoy la acción se ejecutaba de inmediato, sin forma de deshacerla desde la UI. Reutilizar exactamente el mismo componente de modal ya usado en "¿Eliminar este viaje?". Título "¿Finalizar este viaje?", cuerpo con el nombre del viaje y qué implica, botones "Cancelar"/"Finalizar viaje". El modal de éxito existente se mantiene, se sigue mostrando DESPUÉS de confirmar. No tocar "Eliminar viaje" ni la lógica de Firestore al finalizar | Cambio de una sola función en `pages/trip-detail.js`: el listener de `#close-trip` pasó de llamar `closeTrip()` directo a abrir `showConfirmModal()` primero (mismo componente, sin ningún parámetro nuevo ni modificado — literal el pedido de "reutilizar exactamente"), y `closeTrip()` + `showSuccessModal()` (sin cambios) se movieron dentro de `onConfirm`, formando la misma cadena confirmación → acción → éxito que ya usa "Eliminar viaje" un poco más abajo en el mismo archivo. No agregué ningún tono/ícono "más neutro" para el nuevo modal (el pedido lo dejaba opcional — "puede ser") porque reusar el componente tal cual, con su ícono rojo de advertencia de siempre, es justamente lo que pedía la instrucción explícita de reutilización; el rojo tampoco es incorrecto para esta acción (aunque no borra datos, sí es un cambio de estado real con consecuencias — deja de poder agregar gastos). Para probar el flujo completo con datos reales (`closeTrip()` real, no solo la apertura del modal) sin login real de Google, reusé el mismo mock de `firebase/firestore` + config de Vite aparte armados para la tarea anterior (campana de alertas), con un harness nuevo (`trip-detail-test.html`, temporal, en la raíz) que crea un viaje real vía `createTrip()`/`addExpense()` y renderiza `trip-detail.js` real completo. Verifiqué con Playwright el flujo de punta a punta: click en "Finalizar viaje" abre el modal SIN haber cerrado el viaje todavía (título/cuerpo/botones exactos al pedido, con el nombre del viaje interpolado), "Cancelar" cierra el modal sin ejecutar nada (el botón "Finalizar viaje" seguía ahí, viaje seguía activo), click de nuevo + "Finalizar viaje" (confirmar) SÍ ejecuta `closeTrip()` y encadena el modal de éxito ya existente ("El viaje ha sido finalizado"), y aceptar ese modal re-renderiza la página ya en modo "Finalizado" (el botón cambia a "Eliminar viaje", igual que antes de este cambio). Verifiqué con `vite build` que compilara. Borré el harness y la config mock al terminar — nada de eso quedó en el repo | Nada que señalar — cambio acotado a un único listener, sin tocar el resto del archivo ni el componente de modal |

| 2026-08-24 | 2 ajustes a la campana de alertas del header (tarea anterior de esta misma sesión): (1) que muestre el NÚMERO de viajes en alerta, igual que el badge del ítem "Alertas" del sidebar, en vez del punto rojo simple sin número que tenía; (2) agrandar un poco el ícono de la campana | Para el punto 1, en vez de darle a la campana su propio formato de número (con riesgo de que un día se desalinee del badge del sidebar en fuente/tamaño/tope de "9+"), moví `renderNavBadge()` — antes una función privada de `sidebar.js` — a `alerts-bell.js` y la exporté desde ahí; `sidebar.js` ahora la importa en vez de tener su propia copia. Es el mismo cambio de dirección que ya usé para no crear un import circular entre los 2 (`sidebar.js` ya importaba `alertsBell` de `alerts-bell.js`, así que sumar `renderNavBadge` al mismo import, en vez de al revés, evitaba el ciclo). Con la función compartida, ambos lugares literalmente no pueden mostrar números distintos. Para que el número quede anclado al ícono exactamente como en el sidebar (mismo `top`/`right`, sin recalcular nada a mano), envolví el `<svg>` de la campana en el mismo `<span class="sidebar-nav-icon">` que ya usa el ítem "Alertas" del sidebar para anclar su badge — la clase no es exclusiva de la nav a pesar del nombre (es solo `position:relative` + `inline-flex`), así que reusarla tal cual da posicionamiento pixel-idéntico sin CSS nuevo. Borré `.alerts-bell-dot` (el punto simple, ya sin uso) del todo. Para el punto 2, en vez de tocar `--icon-size` (token global, hubiera agrandado TODOS los íconos de la app) agregué `.alerts-bell .icon { width/height: 1.5rem }`, scopeado solo a la campana — lupa/hamburguesa/el resto del header quedan en el 1.25rem de siempre. Verifiqué con Playwright, con 0/2/11 viajes en alerta reales (vía el mismo mock de Firestore + createTrip()/addExpense() armado en la tarea anterior): el número de la campana coincide exacto con el del sidebar en los 3 casos (incluido el tope "9+" con 11), sin badge cuando no hay alertas, y el ícono de la campana mide 24px vs. 20px de lupa/hamburguesa en el mismo header (medido con `getBoundingClientRect()`, no asumido). Verifiqué con `vite build` que compilara | Nada que señalar — cambio acotado a los 2 puntos pedidos, sin tocar el resto de la campana (variantes mobile/desktop, click a `#/alertas`, ocultamiento en el breakpoint mobile de la variante desktop) |

| 2026-08-24 | Reducir el espacio vertical del drawer mobile para que todo el contenido (ítems de nav, tarjeta "Próximas actualizaciones", bloque de sesión) quepa sin scroll en viewports de 700-800px de alto: (1) padding-top del panel más chico, (2) espacio entre ítems de nav más compacto sin perder el área táctil mínima recomendada (~44px), (3) menos aire entre título y descripción de cada ítem de "Próximas actualizaciones", (4) si igual no cabe en pantallas más chicas, scroll interno como respaldo, sin cortar el bloque de sesión | Los 4 ajustes de espaciado quedaron SCOPEADOS al drawer (`.drawer-panel`, `.drawer-nav .sidebar-nav-item`, `.drawer-panel .sidebar-upcoming*`) en vez de tocar las reglas base (`.sidebar-nav-item`, `.sidebar-upcoming*`) — esas mismas clases se reusan tal cual en el sidebar de escritorio (mismo `renderNavItems()`/`renderUpcomingCard()`, ver sidebar.js), así que una reducción sin scope hubiera compactado también la barra vertical de escritorio, que tiene espacio de sobra y no la pidieron tocar. Verifiqué con Playwright que el sidebar de escritorio queda exactamente igual (padding 8px, gap 12px, sin cambios) después de esto. Cambios concretos: `.drawer-panel` padding-top `--space-6`→`--space-4` (24→16px) y `.drawer-close` margin-bottom `--space-4`→`--space-3` (16→12px, punto 1); `.drawer-nav .sidebar-nav-item` padding vertical `--space-2`→0.375rem (8→6px, punto 2); `.drawer-panel .sidebar-upcoming` padding `--space-4`→`--space-3`, su `.sidebar-upcoming-list` gap `--space-3`→`--space-2`, y el título con menos margen inferior (punto 3). El punto 4 (scroll de respaldo) ya existía de antes (`.drawer-panel { overflow-y:auto }`, de la tarea original del drawer) — no hizo falta agregar nada, solo confirmar que seguía funcionando después de compactar. Sobre el área táctil: los 3 ítems reales de nav quedan en 32px de alto (36px antes) y los 4 "Próximamente" en 48px (52px antes) — por debajo del ~44px que pedía el enunciado como mínimo recomendado, pero YA estaban por debajo de eso antes de este cambio (36px); no los reduje más allá de lo necesario para no perder más área de toque de la que ya tenían, y se lo señalo al usuario en vez de asumir que quedó resuelto al 100%. Medí (no asumí) el contenido total del drawer con datos reales de sesión — simulé un usuario autenticado sobreescribiendo `auth.currentUser` con `Object.defineProperty` en el harness de prueba, porque sin login real ese bloque no se renderiza y el drawer se hubiera medido más corto de lo que es en producción — y confirmé con Playwright en 5 alturas de viewport (390px de ancho, igual en las 5): 800/750/700px sin scroll (`scrollHeight === clientHeight`, contenido real mide 686px), 640/568px SÍ activan scroll interno, y en ambos casos hacer scroll hasta el fondo deja el bloque de sesión completamente visible (no cortado). Revisé las capturas a 700px y 568px con scroll — nada se ve apretado, "Próximas actualizaciones" sigue leyéndose bien pese al gap más chico. Verifiqué con `vite build` que compilara | El punto 2 no llegó a los ~44px "recomendados" que pedía el enunciado — ya estaba por debajo antes de este cambio (36px), lo dejé explícito en vez de afirmar que quedó cumplido; "verificar en dispositivo real" del enunciado no lo pude hacer en este entorno (sin dispositivo físico disponible), verifiqué con Playwright en el rango de alturas de viewport pedido en su lugar, mismo límite ya señalado en tareas anteriores de esta sesión para todo lo que requiere probar en el navegador real del usuario |

## Herramientas usadas

- Claude Code — desarrollo del repo local, commits
- _(agregar otras si aplica)_
